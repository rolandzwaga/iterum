# Phase 2 Compliance Report — `seraphis-phase2-harmonic-cloud`

**Phase:** 2 — Harmonic Cloud (Seraphis roadmap)
**Spec slug:** `seraphis-phase2-harmonic-cloud`
**Artifacts:** `specs/seraphis-phase2-harmonic-cloud/{spec.md, plan.md, tasks.md}`
**Component:** `dsp/include/krate/dsp/systems/harmonic_cloud.h` (Layer 3)
**Tests:** `dsp/tests/unit/systems/harmonic_cloud_test.cpp`, `harmonic_cloud_spectral_test.cpp`, `harmonic_cloud_perf_test.cpp`, registered in `dsp/tests/CMakeLists.txt:332-334` (target `dsp_systems_tests`)

## Overall status: **COMPLETE**

Zero fail items, zero partial items. 74 verdicts across three lenses (FR, SC re-measurement, cross-cutting constraints) — all `pass`.

Two items were previously **fail** and one previously **partial**; all three are now closed by evidence read this session, not by assertion:

- **SC-005** — the prior fail rested solely on "the criterion was changed in test code only, the spec was never amended". `spec.md:727` now carries the `AMENDED 2026-07-26 (implementation measurement)` blockquote naming the withdrawn wording verbatim, and `spec.md:758-777` states the replacement clauses, the measured figures and both mandatory positive controls.
- **SC-006** — same pattern; `spec.md:795` carries the amendment blockquote and `spec.md:812-817` states the replacement threshold (`zero detections on the 512-sample block grid`) with the measured `0 on-grid out of totals of 0–13 per channel`.
- **SC-007 / CC-rt** — the prior partial was the CPU budget. `harmonic_cloud_perf_test.cpp:100-119` is now a populated `BASELINE PROVENANCE` block (machine, build, trial shape, date, eight-run table) with no `PROVISIONAL` label, and the checked-in baselines are the worst of those measured figures rounded up. The over-budget WARN no longer fires.

---

## Compliance table — functional requirements (FR lens)

| ID | Verdict | Evidence (verbatim) |
|---|---|---|
| FR-001 | pass | dsp/include/krate/dsp/systems/harmonic_cloud.h:49 declares `class HarmonicCloud` under systems/ (Layer 3); its only krate includes are :17-23 — core/crossfade_utils.h, core/db_utils.h, core/math_constants.h, core/pitch_utils.h, core/random.h (L0), primitives/smoother.h (L1), processors/harmonic_oscillator_bank_simd.h (L2) — plus stdlib :25-29. No L3/L4 include, no harmonic_types.h / harmonic_frame*. ODR sweep `grep -rn "class HarmonicCloud\|struct HarmonicCloud" dsp/ plugins/ tools/` returns exactly one hit (that line). g++ 13 -std=c++20 -O2 -Wall -Wextra compiles the header clean (only the expected link error for processMcfBatchSIMD). |
| FR-002 | pass | Every public method carries `noexcept` (prepare :179, reset :210, all setters :265-475, processStereoBlock :606-607, all FR-008 accessors :678-829). All partial state is fixed-size `std::array` members :1549-1577 — no vector/new anywhere in the header. Measured: HarmonicCloud_NoAllocInProcess (harmonic_cloud_test.cpp:4694) ran "All tests passed (6 assertions in 1 test case)" with a mandatory liveness probe (`REQUIRE(livenessCount >= 1)` :4720) proving the global operator-new overrides are linked, then `REQUIRE(renderCount == 0)` :4789 over 200 automated 512-sample blocks. |
| FR-003 | pass | harmonic_cloud.h:179-207 `prepare(double)` re-derives nyquist_, invSampleRate_, fadeStart_, invFadeRange_, ampSmoothCoeff_ (:187), crossfadeLengthSamples_ (:188), normGain_.configure (:191), driftSmoothCoeff_ (:193) and both banks' AR(1) coefficients (:202-203), then reset(). reset() :210-254 zeroes amplitudes/env/detune/gate and lane state without touching configuration. Sample-rate propagation to the drift lanes is the SoA equivalent of BrownianDrift::prepare (:202-203, :1408-1420). HarmonicCloud_GuardPaths section "Prepare + noteOn + render with no setter call is finite and non-silent" (:218-242) passed — 2052 assertions in 1 test case. |
| FR-004 | pass | harmonic_cloud.h:606-671 `processStereoBlock(float* leftOutput, float* rightOutput, std::size_t numSamples) noexcept`; :609-611 rejects null L, null R and numSamples==0 without writing. HarmonicCloud_GuardPaths section 1 (harmonic_cloud_test.cpp:170-193) poisons both buffers with -12345.0f, issues all three calls and asserts `firstWritten == kBlockSize` and the drift read count is unchanged — passed. |
| FR-005 | pass | harmonic_cloud.h:1597-1598 two `Xorshift32` streams (configRng_, phaseRng_); setSeed :589-594; deriveSeed :575-584 (lowbias32 with an explicit non-zero substitution so no lane collapses onto Xorshift32's default-seed substitution). HarmonicCloud_SeedDerivationIsDistinct passed — 33792 assertions in 1 test case, asserting the 128 derived streams are pairwise distinct and non-zero including for cloud seed 0. |
| FR-006 | pass | harmonic_cloud.h:93 `kOutputClamp = 2.0f`; :663-664 both channels `std::clamp(s, -kOutputClamp, kOutputClamp)` on every sample. Finiteness uses bit-pattern `detail::isNaN/isInf` (:266 etc., :782-791 stateFinite), never std::isnan. Measured by SC-017's fully enumerated 3888-cell grid (harmonic_cloud_test.cpp:3311 `static_assert(kSc017CellCount == 3888)`), 1 s per cell at 48 kHz: `REQUIRE(scan.worstMagnitude <= kOutputClamp)` :3470 plus per-sample bit-pattern scan :3375-3381 — All tests passed (11951 assertions). |
| FR-007 | pass | Every setter takes the finite-first reject path before any state write, e.g. setFundamentalHz :266-268, setRichness :295-297, setInharmonicity :309-311, setSpectralTiltDb :322-324, setMutation :335-337, setSpectralGravity :361-363, setDriftDepthCents :384-386, setDriftSmoothness :396-398, setStereoSpread :418-420, setAttackTimeSec :439-441, setDecayTimeSec :451-453, setEnvelopeOffsetSpread :463-465, setPartialPosition :801-803; each then `std::clamp`s to a documented range. SC-017 pass 2 (harmonic_cloud_test.cpp:3478) feeds NaN/±Inf built from bit patterns through a volatile sink (:3323-3328, no std::numeric_limits) to every setter and asserts the getter is bit-identical and the subsequent render matches the pre-call fingerprint; pass 3 (:3573) asserts finite out-of-range values come back as the documented bound. Passed. |
| FR-008 | pass | harmonic_cloud.h:678-829 exposes the full required surface: getActivePartialCount :678, getPartialFrequencyHz :683, getPartialCurrentAmplitude :687, getPartialTargetAmplitude :691, getPartialUnmutatedTargetAmplitude :697, getPartialPanLeft/:705, getPartialPanRight :709, getPartialPosition :714, getPartialDriftDetune :719 (frequency multiplier), setPartialPosition :797-807 (clamps position, no-ops out-of-range index), setPartialMask :812 / soloPartial :820 / clearPartialMask :829 (mask applied at the END of the chain :1295 so FR-014's smoother still fades it). All const-noexcept except setPartialPosition/mask, all public contract (no #ifdef). |
| FR-011 | pass | harmonic_cloud.h:649-653 calls `processMcfBatchSIMD(sinState_.data(), cosState_.data(), epsilon_.data(), detuneMultiplier_.data(), currentAmplitude_.data(), targetAmplitude_.data(), antiAliasGain_.data(), panLeft_.data(), panRight_.data(), ampSmoothCoeff_, kernelCount_, sl, sr)` — the kernel's exact parameter contract. sinState_/cosState_ are seeded, not zeroed, via redrawPhases :880-886. Frequency accuracy of the resulting MCF orbit measured by HarmonicCloud_PartialFrequencyAccuracyWithin0p1Cent (passed, 20 assertions). |
| FR-012 | pass | harmonic_cloud.h:60 `static constexpr std::size_t kMaxPartials = 64;` (class-scoped to avoid the namespace-scope kMaxPartials=96 in harmonic_types.h:21). SoA arrays declared `alignas(32)` :1549-1571. No runtime partial-count-change API exists anywhere in the header. The kernel is called with `kernelCount_` (:653), and the kernel itself uses hn::LoadU/StoreU so any count is safe; nothing in the header assumes alignment (documented :1543-1546). |
| FR-013 | pass | harmonic_cloud.h:265-285 setFundamentalHz clamps to kMinFundamentalHz=20 / kMaxFundamentalHz=4000 (:103-104); on ratio > crossfadeThresholdRatio_ (= semitonesToRatio(1.0f), :190) it snapshots lastOutL_/lastOutR_ and arms crossfadeRemaining_ (:278-282), applied at :655-661 over kCrossfadeTimeSec=0.003f (:107). Only freqDirty_ is raised (:284) — phase accumulators are untouched. Measured by HarmonicCloud_MacroSweepsAreClickFree's 220→440 Hz one-octave step clause (harmonic_cloud_test.cpp:4356-4359), passed. |
| FR-014 | pass | harmonic_cloud.h:187 `ampSmoothCoeff_ = 1.0f - std::exp(-1.0f / (kAmpSmoothTimeSec * sr))` with kAmpSmoothTimeSec = 0.002f (:84), passed as the kernel's single scalar at :652. It is the last stage: everything (richness, tilt, mutation weight, envelope, mask) is written into targetAmplitude_ (:1295) upstream of it. Shortest supported attack kMinAttackSec = 0.05 s (:137) = 25× the 2 ms constant, clearing FR-023's ≥20× rule. |
| FR-015 | pass | harmonic_cloud.h:1118-1133 updateAntiAliasGain: full gain below fadeStart_ = 0.8·Nyquist (:87, :185), linear fade `(nyquist_ - fEff) * invFadeRange_` to 0 at Nyquist, times the MCF orbit correction `sqrt(1-(eps/2)^2)` (:1120-1122, the exact identity for cos(pi f/fs), evaluated on the DETUNED epsilon — documented deviation plan D5 at :1099-1106). Epsilon clamped to ±kMaxEpsilon=1.99f at :990-991. Measured by HarmonicCloud_NoAliasingAtExtremes, passed (16 assertions), threshold `REQUIRE(m.aliasDb - m.fundamentalDb <= -60.0f)` at harmonic_cloud_spectral_test.cpp:1190. |
| FR-016 | pass | harmonic_cloud.h:880-886 redrawPhases draws phi_i = phaseRng_.nextUnipolar()*kTwoPi and sets sinState_=sin(phi), cosState_=cos(phi); distribution documented :871-874. Retrigger rule at :517-542: redraw only when isQuiescent() (gate off AND every kernel-visible currentAmplitude < kQuiescenceAmplitude = 1.0e-5f, :146, :768-778); a sounding note-on keeps phases and only re-opens envStage_. Measured by HarmonicCloud_RetriggerIsClickFree (passed, 1428 assertions) — section :3108 asserts every MCF state is untouched across a sounding retrigger, section :3184 asserts the quiescent path DOES redraw. |
| FR-017 | pass | harmonic_cloud.h:1342-1353 currentNormGainTarget = min(kTargetOscRms(0.5f, :96) / sqrt(sum a_i^2 * 0.5), kMaxNormGain(20.0f, :97)) over baseAmplitude_ only. baseAmplitude_ is richnessRolloff × tiltGain and nothing else (:1045). Target is set as the last statement of recalculateAmplitudes (:1061) into a cloud-level OnePoleSmoother normGain_ (:1595, kNormGainSmoothMs=20, :100). The chain is `unmutatedTarget_[i] = gainSmoothed_ * baseAmplitude_[i]` (:1254) then `targetAmplitude_[i] = unmutatedTarget_[i] * w * env` (:1295) — mutation, drift and envelope are provably outside the normalizer input. Verified by SC-003's `REQUIRE(measured[t].peak < 0.9f * kOutputClamp)` (harmonic_cloud_spectral_test.cpp:1098) at ±12 dB/oct and SC-018's onset peak clause, both passed. |
| FR-021 | pass | harmonic_cloud.h:906 `positionScatter_[i] = configRng_.nextFloat()` (already bipolar [-1,1], drawn once per seed in reseed()); :1084-1091 recalculatePan sets `panPosition_[i] = stereoSpread_ * positionScatter_[i]` unless overridden. Override scope enforced: setStereoSpread clears positionOverridden_ (:427), setSeed clears it (:591), reset() clears it (:228). Measured by HarmonicCloud_EqualPowerPanAndSpread (passed, 1881 assertions), spread section harmonic_cloud_test.cpp:939-1041. |
| FR-022 | pass | harmonic_cloud.h:916-920: `u = 0.5f + 0.5f*configRng_.nextUnipolar()` (U[0.5,1.0]) and `driftAmount_[i] = std::pow((i+1)/kMaxPartials, kDriftIndexExponent) * u` with kDriftIndexExponent = 1.0f (:130); denominator is the fixed capacity, not activeCount_. Measured by SC-015 clause 3 (harmonic_cloud_test.cpp:1762-1913): index scaling `REQUIRE(highMean >= 4.0 * lowMean)` and seeded scatter `REQUIRE(inversions >= 1)` — HarmonicCloud_DriftIsIndependentDecimatedAndBounded passed, 295 assertions. |
| FR-023 | pass | harmonic_cloud.h:1166-1195 advanceEnvelope implements exactly linear AR: Attack `envValue_ += dt/(attackSec_ + attackOffsetSec_[i])` clamped to 1 then Hold; Release `envValue_ -= dt/(decaySec_ + decayOffsetSec_[i])` to 0 then Idle. No sustain, no separate release stage. Offsets are seeded draws scaled by offsetSpread_ × kMaxEnvOffsetSec (2.0 s, :143) at :951-957, re-scaled not re-drawn on a spread change. Evaluated once per control chunk by the cloud and written into targetAmplitude_ (:1285, :1295). kMinAttackSec = 0.05 s (:137) = 25× kAmpSmoothTimeSec. Measured by HarmonicCloud_PartialEnvelopeOffsetsStagger, passed (1417 assertions). |
| FR-024 | pass | harmonic_cloud.h:1560-1571 — frequencyHz_, baseAmplitude_, unmutatedTarget_, panPosition_, positionScatter_, driftAmount_, attackOffsetSec_, decayOffsetSec_, attackOffsetDraw_, decayOffsetDraw_, envValue_ all `alignas(32) std::array<float, kMaxPartials>`, parallel to the kernel-facing arrays at :1549-1557. (The two non-float bookkeeping arrays positionOverridden_/masked_ :1565,:1573 and envStage_ :1572 are not alignas(32); they are never SIMD-consumed, so the SIMD-friendly-layout intent is met.) |
| FR-031 | pass | harmonic_cloud.h:853-862 DriftLanes SoA transposition; coefficients :1408-1420 (tau = lerp(0.2,30), a = exp(-dt/tau), g = kInternalStd*sqrt(1-a^2), double intermediates); OU step :1433-1452 with three explicitly sequenced nextFloat() draws, ±kDriftWalkLimit clamp and kDenormalFloor flush; output target clamp(depth*x, ±1) :1450; 150 ms smoother transcribed from OnePoleSmoother::advanceSamples including isComplete() skip, flushDenormal and the post-advance kCompletionThreshold snap :1490-1507. Two banks, 128 lanes (:1576-1577) with distinct derived seeds (:937-942). Equivalence is MEASURED, not asserted: HarmonicCloud_DriftLaneMatchesBrownianDrift passed with 270012 assertions (60 s, smoothness {0,0.5,1}, both banks, 1e-5 tolerance per chunk). |
| FR-032 | pass | harmonic_cloud.h:63 kControlChunkSamples = 64; render loop :637-640 subdivides any block into ceil(n/64) chunks and calls updateControl(chunk); each lane's smoothed value is read exactly once per chunk (:1243 detune, :1266 mutation) and the multiplier held constant; the bank itself advances by exactly chunkLength at its own 32-sample OU interval (:1521-1533). driftReadCount_ increments once per chunk (:1222) and identically on the quiescent early-out (:628). Measured by SC-015 clause 2 (harmonic_cloud_test.cpp:1605-1761): reads == Σceil(blockSize/64) for 1×N, 512- and 577-sample schedules, lane state agrees within 1e-5, and the single-block vs 512-block renders match under render_fingerprint tolerances. Passed. |
| FR-033 | pass | harmonic_cloud.h:1243-1245: `d = clamp(detuneLanes_.smoothCur[i], -1, 1); cents = driftCents_ * driftAmount_[i] * d; detuneMultiplier_[i] = semitonesToRatio(cents / 100.0f)` — exactly the specified law, with kMaxDriftCents = 50.0f (:133) as the documented cloud-level maximum. Measured by SC-015 clause 3a/3b (harmonic_cloud_test.cpp:1762+): `REQUIRE(overallMax <= kDepthCents + 0.1)` and `REQUIRE(overallMax >= 0.25 * kDepthCents)`, plus exactly-0 detune at depth 0. Passed. |
| FR-034 | pass | harmonic_cloud.h:1247-1252 — epsilon_ is deliberately NOT recomputed on a detune change; the kernel forms eps*detune itself, so sinState_/cosState_ are never stepped discontinuously. Only the FR-015 gain follows (:1252). Zipper-freedom of the chunk-rate update is measured by HarmonicCloud_NoZipperUnderMutationAndDrift's on-grid enrichment clause (harmonic_cloud_test.cpp:4121,4129) — detections are NOT concentrated on the 64-sample control grid, with a synthetic-zipper positive control that IS (≥90% on-grid, :4145). Passed. |
| FR-035 | pass | harmonic_cloud.h:383-407 — setDriftDepthCents writes only driftCents_ (consumed at :1244, detune path only) and setDriftSmoothness calls `updateDriftCoefficients(detuneLanes_, driftSmoothness_)` only (:406). The mutation bank is configured at the fixed kMutationSmoothness (:127) in prepare (:203) and its `depth` stays at the DriftLanes default 1.0 (:860) — no code path writes mutationLanes_.depth. Measured by SC-015 clause 4 (harmonic_cloud_test.cpp:1914+): after one cloud-level call all 64 detune lanes respect the new bound and `REQUIRE(worstMutationDelta == 0.0f)`. Passed. |
| FR-041 | pass | harmonic_cloud.h:1031-1032 `activeCount_ = clamp(int(round(pow(64, richness_))), 1, 64)` and :1036-1037 `exponent = 3.0 + (0.5 - 3.0)*richness_` = 3.0 - 2.5r; :1045 `baseAmplitude_[i] = pow(n, -exponent) * tiltGain(n)`; inactive partials get 0 (:1041) and activeCount_ is what the kernel receives via kernelCount_. Measured by SC-014 (harmonic_cloud_spectral_test.cpp:967): `REQUIRE(m.activeCount == kExpectedCount[s])` with kExpectedCount {1,3,8,23,64} at r {0,0.25,0.5,0.75,1} (:972), and per-partial `REQUIRE(\|relativeDb - (-20*p*log10(n))\| <= 0.5f)` (:1011). Passed, 452 assertions. |
| FR-042 | pass | Measured by HarmonicCloud_RichnessAddsPartialsAndEnergy (passed, 452 assertions): monotone non-decreasing in both metrics at harmonic_cloud_spectral_test.cpp:1035-1036, endpoint discrimination `REQUIRE(countAtOne - countAtZero >= 16)` :1043 and `REQUIRE(fractionAtOne - fractionAtZero >= 20.0f)` :1048, with the audibility floor pinned at kAudibilityFloorDb = -60.0f (:77) and the energy fraction floored at -80 dB (:81). |
| FR-043 | pass | harmonic_cloud.h:1048-1053 — a partial dropped from the active count keeps a zero target but stays inside kernelCount_ via tailHighWater_, so the kernel's uniform ampSmoothCoeff fades it rather than truncating; retireFadedTail :1070-1081 drops the high-water mark only once every departing lane is below kTailSilenceThreshold = 1e-8 (:149). Measured by the Richness sweep of HarmonicCloud_MacroSweepsAreClickFree (passed) and HarmonicCloud_DecaysToExactZero (passed, 450 assertions). |
| FR-051 | pass | harmonic_cloud.h:987 `const float stretch = std::sqrt(1.0f + inharmonicity_ * n * n);` used at :988 as f_n = f0 * ratioG * stretch, i.e. f0·n·sqrt(1+B n^2) at gravity 0 (ratioG == n exactly, :986). Measured by HarmonicCloud_InharmonicityFollowsPianoLaw (passed, 35 assertions) at B ∈ {0.01, 0.05, 0.1} × n ∈ {1,2,4,8,16,32}, threshold ≤1 cent (harmonic_cloud_test.cpp:322-448). |
| FR-052 | pass | harmonic_cloud.h:110 `kMaxInharmonicity = 0.1f`, clamped at :312 `std::clamp(B, 0.0f, kMaxInharmonicity)`; static_assert pins it in the test TU (harmonic_cloud_test.cpp:3294). FR-015 compliance at the maximum with 64 partials and f0 = 4000 Hz is measured by SC-011 (harmonic_cloud_spectral_test.cpp:407 kSc011F0Hz = kMaxFundamentalHz, max inharmonicity and max \|gravity\|), which passed at ≤ -60 dB. |
| FR-053 | pass | harmonic_cloud.h:316-317 setInharmonicity writes the shadow and raises freqDirty_ only; the deferred recalculateFrequencies (:974-993) writes frequencyHz_/epsilon_ and never touches sinState_/cosState_ (documented :972-973). Amplitude path is untouched. Measured by the Inharmonicity sweep of HarmonicCloud_MacroSweepsAreClickFree (passed, 26 assertions). |
| FR-061 | pass | harmonic_cloud.h:999-1004 `tiltGain(n) = (tiltDb_ == 0 \|\| n <= 1) ? 1.0f : std::pow(10.0f, tiltDb_ * std::log2(n) / 20.0f)` — the additive_oscillator.h:480-490 law with its identity branch. Applied at :1045. Measured by HarmonicCloud_TiltSlopeMatchesSetting (passed, 346 assertions): differential fitted slope within 0.5 dB/oct (harmonic_cloud_spectral_test.cpp:1117), tilt-0 slope within 0.5 dB/oct of -5.52 (:1107), per-partial gain within 0.5 dB (:1137). |
| FR-062 | pass | harmonic_cloud.h:113-114 kMinTiltDbPerOct = -12.0f / kMaxTiltDbPerOct = 12.0f, clamped at :325; static_asserts pin both in the test TU (harmonic_cloud_test.cpp:3296-3297). Tilt 0 leaves amplitudes unmodified by the explicit identity branch at :1000. |
| FR-063 | pass | Tilt enters baseAmplitude_ (harmonic_cloud.h:1045) → unmutatedTarget_ (:1254) → targetAmplitude_ (:1295), which is the kernel's smoothed target; nothing writes currentAmplitude_ directly. setSpectralTiltDb raises only ampDirty_ (:330). Measured by the Tilt sweep of HarmonicCloud_MacroSweepsAreClickFree, passed. |
| FR-071 | pass | harmonic_cloud.h:1275-1277 `w = (mutationAmount_ <= 0) ? 1.0f : 1.0f + mutationAmount_ * kMaxMutationDepth * dm` with kMaxMutationDepth = 0.75f (:124) and dm the clamped MUTATION-bank value (:1266), never the detune value d (:1243). Applied at :1295 downstream of the FR-017 normalizer. Measured by HarmonicCloud_MutationStaysBoundedAndLevelStable (passed, 676 assertions): exactly 1.0 at Mutation 0 (`REQUIRE(run.minWeight[i] == 1.0)`), rate bound \|Δw\| ≤ 10/s × blockDuration (kMaxWeightRatePerSecond = 10.0). |
| FR-072 | pass | harmonic_cloud.h:1577 `DriftLanes mutationLanes_`; seeds `deriveSeed(configuredSeed_, i + kMaxPartials)` (:941), disjoint from the detune bank's `deriveSeed(configuredSeed_, i)` (:938); depth stays at the DriftLanes default 1.0 (:860, never written by any setter); smoothness fixed at kMutationSmoothness = 0.5f (:127) configured only in prepare (:203); advanced on the same chunk cadence (:1217) and read at :1266. Measured by SC-016 sections 2 and 3 (harmonic_cloud_test.cpp:2453, :2505): at drift depth 0 / Mutation 1 the weights still move for ≥ half the active partials and detune is exactly 0; at drift max / Mutation 0 every weight is exactly 1.0 while detune moves. Passed. |
| FR-073 | pass | Bound is exact by construction: \|dm\| ≤ 1 (clamp at harmonic_cloud.h:1266) × kMaxMutationDepth 0.75 gives w ∈ [0.25, 1.75]. Measured by HarmonicCloud_MutationStaysBoundedAndLevelStable (passed, 676 assertions): `REQUIRE(run.minWeight[i] >= kMinMeasuredWeight)` / `<= kMaxMeasuredWeight` with kMinLegalWeight = 0.25 / kMaxLegalWeight = 1.75 (harmonic_cloud_test.cpp:2280 block) and `REQUIRE(std::abs(worstDb) <= 3.0)` for the 1 s-window RMS against the Mutation-0 reference. |
| FR-074 | pass | The weight changes at most once per 64-sample control chunk (harmonic_cloud.h:1266 inside updateControl, called only at :640) and reaches the oscillator only through targetAmplitude_ (:1295) and the kernel's ampSmoothCoeff. Block-size invariance of the weight trajectory follows from the same chunk loop verified by SC-015 clause 2. Zipper-freedom measured by HarmonicCloud_NoZipperUnderMutationAndDrift at Mutation 1.0 (passed); the test records mutation-alone at 0 detections over the 30 s render (harmonic_cloud_test.cpp:4207 comment). |
| FR-081 | pass | harmonic_cloud.h:975 `exponent = 1.0f + gravity_ * kGravityExponentRange` with kGravityExponentRange = 0.1f (:117); :986 `ratioG = gravityIsZero ? n : std::pow(n, exponent)` — the g == 0 identity branch is explicit so the integer grid is exact under -ffast-math (:976-982). pow(1, x) == 1 keeps the fundamental fixed. setSpectralGravity clamps to [-1,+1] (:364). Measured by SC-004 part 1 (harmonic_cloud_test.cpp:~640 `REQUIRE(std::abs(gridDeviation[kIdxZero][pi]) < 0.1)` cents) — HarmonicCloud_GravityMapsMonotonically passed, 201 assertions. |
| FR-082 | pass | Measured by HarmonicCloud_GravityMapsMonotonically (passed, 201 assertions) over the 5-setting sweep {-1,-0.5,0,+0.5,+1}: strict monotonicity in \|g\| with each step ≥ 5× the 0.1-cent estimator tolerance (`REQUIRE(meanCents[kIdxPlusHalf] >= meanCents[kIdxZero] + kMinStepCents)` and the three sibling assertions), and sign restricted to n ≥ 2 (`REQUIRE(devPlus * devMinus < 0.0)`, `devPlus > 0`, `devMinus < 0`, with `REQUIRE(qualifying == partialsAboveFundamental)`). |
| FR-083 | pass | harmonic_cloud.h:988 `const float f = fundamentalHz_ * ratioG * stretch;` — gravity warp first, inharmonicity stretch second, exactly the specified order; documented in the doxygen block :963-971. Measured by SC-004 part 4 (harmonic_cloud_test.cpp, kCompositionInharmonicity = 0.05 at \|g\| = max): `REQUIRE(std::abs(m.lawCents) <= kCombinedLawToleranceCents)` with the 1-cent bound. Passed. |
| FR-084 | pass | harmonic_cloud.h:368-369 setSpectralGravity raises only freqDirty_; the deferred recalculateFrequencies (:974-993) writes frequencyHz_/epsilon_ and never sinState_/cosState_ — the identical mechanism FR-053 uses, documented at :356-359. Measured by the Gravity sweep of HarmonicCloud_MacroSweepsAreClickFree, passed. |
| FR-091 | pass | harmonic_cloud.h:1363-1378 updatePanGains performs the mandatory bipolar→[0,1] remap `p01 = std::clamp((panPosition_[i] + 1.0f) * 0.5f, 0.0f, 1.0f)` BEFORE calling equalPowerGains, then floors both gains at 0 to kill the ~4.4e-8 negative endpoint kHalfPi produces. Measured by SC-012 clauses 1-4 (harmonic_cloud_test.cpp:860-923) over the pinned grid {-1,-0.5,0,+0.5,+1} read back through the shipped accessors: \|L²+R²-1\| ≤ 1e-6, L ≥ 0 and R ≥ 0, L strictly decreasing / R strictly increasing, \|L-R\| ≤ 1e-6 at centre. Passed, 1881 assertions. |
| FR-092 | pass | harmonic_cloud.h:1556-1557 `alignas(32) std::array<float, kMaxPartials> panLeft_{}; panRight_{};` passed as the kernel's panLeft/panRight arguments at :652; stereo accumulation happens inside processMcfBatchSIMD (the cloud only zeroes sl/sr per sample at :646-647 and reads the accumulated result, :649-653). |
| FR-093 | pass | At stereoSpread_ = 0 recalculatePan sets every panPosition_ to 0 (harmonic_cloud.h:1087) → p01 = 0.5 → identical L/R gains. Measured by SC-012's spread section (harmonic_cloud_test.cpp:939-1041): `REQUIRE(worstCentreDelta <= 1.0e-7)` over the FULL spread-0 render including onset, and `REQUIRE(correlation[s] < correlation[s-1])` strictly across spreads {0, 0.25, 0.5, 0.75, 1.0}. Passed. |

## Compliance table — success criteria, FR-lens evidence

| ID | Verdict | Evidence (verbatim) |
|---|---|---|
| SC-001 | pass | HarmonicCloud_PartialFrequencyAccuracyWithin0p1Cent — "All tests passed (20 assertions in 1 test case)". Test matrix is exactly the spec's (harmonic_cloud_test.cpp:264-275): n∈{1,8,32,64}@55 Hz, {1,8,32}@440 Hz, {1,8,16}@1 kHz, with `REQUIRE(fExpected < 0.8 * kNyquist)` asserted per case (:287). Threshold `REQUIRE(std::abs(cents) < 0.1)` (:311) on `100 * ratioToSemitones(fEst/fExpected)` (:307-308). Estimator resolution is separately proven: HarmonicCloud_FrequencyEstimatorResolution passed (4 assertions) with `REQUIRE(std::abs(cents) < 0.001)` (:159) at 55/440/3520/7040 Hz — 100× finer than 0.1 cent, exceeding the spec's 10× requirement. |
| SC-002 | pass | HarmonicCloud_InharmonicityFollowsPianoLaw — "All tests passed (35 assertions in 1 test case)". 3 non-zero B values {0.01, 0.05, 0.1} (harmonic_cloud_test.cpp:329) × partials {1,2,4,8,16,32} (:330), 10 s renders at 48 kHz, same estimator; the 0.8·Nyquist constraint is asserted per measured partial. Threshold ≤ 1 cent, three orders above the 0.001-cent documented estimator resolution. |
| SC-003 | pass | HarmonicCloud_TiltSlopeMatchesSetting — "All tests passed (346 assertions in 1 test case)". Configuration matches the pinned spec exactly: kSc003Richness = 5/6 → 32 partials (harmonic_cloud_spectral_test.cpp:237-238), f0 = 110 Hz (:43), 48 kHz, kFftSize = 65536 (:51) with Window::generateBlackmanHarris (:157), ≥4 s render. Unclipped precondition `REQUIRE(measured[t].peak < 0.9f * HarmonicCloud::kOutputClamp)` (:1098). Thresholds: `REQUIRE(std::abs(slopeZero - (-5.52f)) <= 0.5f)` (:1107), `REQUIRE(std::abs(differentialSlope - tilt) <= 0.5f)` (:1117), `REQUIRE(std::abs(residual[i] - offset) <= 0.5f)` (:1137). |
| SC-004 | pass | HarmonicCloud_GravityMapsMonotonically — "All tests passed (201 assertions in 1 test case)". All four parts present: (1) grid at g=0 within 0.1 cent; (2) 5 settings {-1,-0.5,0,+0.5,+1} with each step ≥ kMinStepCents = 5 × 0.1 cent (harmonic_cloud_test.cpp:460, kMinStepCents); (3) sign inversion restricted to n ≥ 2 with `REQUIRE(qualifying == partialsAboveFundamental)`; (4) composition order at B = 0.05, `REQUIRE(std::abs(m.lawCents) <= 1.0)` against f0·n^(1+0.1g)·sqrt(1+Bn²). |
| SC-005 | pass | Previously-failing cause REMOVED: the spec IS now amended. specs/seraphis-phase2-harmonic-cloud/spec.md:727 carries a blockquote '> **AMENDED 2026-07-26 (implementation measurement).** The pass condition below replaces an earlier one that a correct implementation cannot satisfy. The withdrawn wording was: 1. detections(modulated) <= detections(control), and 2. maxPerSampleDelta(modulated) <= 1.5 x maxPerSampleDelta(control)', and spec.md:758 states the replacement '*Pass condition (differential, as amended).*' with clause 1 at :762 ('No control-grid signature ... must not exceed 2.25x the uniform expectation') and clause 2 at :767 ('Matched-regime slew ... slowControl ... drift smoothness set to 1.0'). The earlier verdict rested solely on 'the criterion was changed in test code only, the spec was never amended' — that is no longer true; both amended clauses, both measured figures, and both mandatory positive controls are written into spec.md:762-777. RAN THIS SESSION: build/windows-x64-release/bin/Release/dsp_systems_tests.exe "HarmonicCloud_NoZipperUnderMutationAndDrift" --success -> 'All tests passed (24 assertions in 1 test case)', exit 0. Measured values match the amended spec text verbatim: clause 1 'left channel: 6 of 126 detections on the 64-sample control grid (4.7619%), uniform = 6.25%, limit = 14.0625%' and 'right channel: 5 of 141 detections on the control grid (3.5461%), limit = 14.0625%' (asserts at harmonic_cloud_test.cpp:4256 and :4268); clause 2 'left channel: max \|dx\| fast drift = 0.305248, slow drift = 0.278125, bound = 0.417187' (ratio 1.0975) and 'right channel: max \|dx\| fast drift = 0.32557, slow drift = 0.298882, bound = 0.448324' (ratio 1.0893), asserts at harmonic_cloud_test.cpp:4317 and :4324. Both mandatory positive controls fire: 'positive control: injected a 0.341992 step (2 x the control's own max \|dx\|) at sample 720000; 1 detection(s)' (REQUIRE(detectionsAtInjection >= 1), :4130) and 'synthetic zipper positive control: 166 detections, 166 on-grid' (REQUIRE >= 32, REQUIRE >= 0.9, :4282/:4285). The three withdrawn-clause guards demanded by spec.md:753-755 ('pinned by a standing assertion ... phrased as the negation of the withdrawn clause') are all present and all fired: 'frozen control scores 0 detections (both channels), the modulated render 267' (REQUIRE(controlDetections < modulatedDetections), :4205), 'max \|dx\| ratio against the FROZEN control = 1.78512' (REQUIRE(withdrawnRatioL > 1.5), :4218), and 'SC-005's literal 10%-of-peak injection is 0.0747897' vs max \|dx\| 0.170996 (REQUIRE(specInjectStep < controlDeltaForInjection), :4157). |
| SC-006 | pass | Previously-failing cause REMOVED: spec.md:795 now carries '> **AMENDED 2026-07-26 (implementation measurement).** The withdrawn threshold was **detections(swept) <= detections(control)** ... It is not a criterion, because its verdict is decided by a choice the wording never makes', and spec.md:812-816 states the replacement: 'Threshold: **zero detections on the 512-sample block grid** (+/-1 sample) for each of the five macro sweeps, on both channels, and for the fundamental step.' RAN THIS SESSION: dsp_systems_tests.exe "HarmonicCloud_MacroSweepsAreClickFree" --success -> 'All tests passed (28 assertions in 1 test case)', exit 0. All twelve on-grid assertions (6 renders x 2 channels, REQUIRE(onGrid == 0) at harmonic_cloud_test.cpp:4525) measured ZERO on-grid: totals per channel were 0, 1, 2, 5, 8 and 13 across the Richness / Inharmonicity / Tilt / Mutation / Gravity sweeps, and 'fundamental step 220 -> 440 Hz / left channel: 0 of 1 detections on the 512-sample block grid' + 'right channel: 0 of 1' — exactly the '0 on-grid out of totals of 0-13 per channel' spec.md:816-817 records. Both mandatory positive controls fire, including the literal SC-005 peak form that SC-005 itself could not use: 'positive control: injected a 0.1797 step (2 x the control's own max \|dx\|) at sample 120064; 1 detection(s)', 'SC-006 positive control (literal SC-005 form): injected a 0.0828493 step (10% of the control's peak 0.828493) ... 1 detection(s)' (:4450), and 'synthetic per-block zipper: 22 detections, 22 on the block grid' (REQUIRE >= 16 and >= 90% on-grid, :4469/:4470). The withdrawn-clause guard stands and fired: 'a STATIC render at the Richness sweep's min endpoint scores 0 detections and at its max endpoint 43 - both are the frozen control of the same configuration', REQUIRE(endpointSpread >= 16) at :4509. Retrigger clause — the one clause SC-006 KEPT as a count comparison (spec.md:820-830) — verified separately: dsp_systems_tests.exe "HarmonicCloud_RetriggerIsClickFree" -> 'All tests passed (1428 assertions in 1 test case)', measuring 'left channel: retriggered = 3 detections, control = 3' and 'right channel: retriggered = 4 detections, control = 4' (meets detections(retriggered) <= detections(control)), plus the mechanism: 'MCF states changed across a sounding retrigger: 0 of 64 (must be 0)' and the non-vacuity case 'MCF states changed across a quiescent note-on: 64 of 64' after 'reached quiescence after 11 blocks (cap 60)'. |
| SC-007 | pass | BOTH previously-violated clauses are now satisfied, and the fix was cost reduction, not baseline inflation. (1) BASELINE PROVENANCE — the 'PROVISIONAL - NOT YET MEASURED ON THIS MACHINE' labels are gone. harmonic_cloud_perf_test.cpp:100-119 is a 'BASELINE PROVENANCE (SC-007, spec.md "Baseline provenance")' block recording exactly what spec.md:874-878 demands: 'Machine : Windows 11 Pro 26200, 13th Gen Intel(R) Core(TM) i9-13900HX / Build : MSVC Release, build/windows-x64-release / Trial shape: best-of-25 x 500 blocks / Measured : 2026-07-26', followed by an eight-run table (static min 20641 max 23154; automated min 21917 max 25262; quiescent min 9166 max 10998). kStaticBaselineNsPerBlock = 24000.0 (:123) and kAutomatedBaselineNsPerBlock = 26000.0 (:140) are the worst of those figures rounded up. (2) OVER-BUDGET — resolved. Cost was cut via three levers documented at :86-98 with measurements (31281-32027 -> 20641-23154 static; 33257-34184 -> 21917-25262 automated): detail::centsToDriftRatio (harmonic_cloud.h:106-112, a bounded-domain degree-4 Horner replacing std::pow, 512 calls/block), detail::kHarmonicCloudLog2N, and DriftLanes::cachedPowN (harmonic_cloud.h:1620-1628). :137-139 states the discipline was followed: 'the first honest measurement of this component was 35,052 static / 37,002 automated, over the 35,556 ceiling.' RAN THIS SESSION, three separate invocations of dsp_systems_tests.exe "HarmonicCloud_CpuBudget*" (each 'All tests passed (10 assertions in 2 test cases)'): static 20114.6 / 25116.4 / 27628.4 ns per 512-block = 0.188574% / 0.235466% / 0.259016% of budget; automated (GATING) 22180.8 / 26602.6 / 29642.8 ns = 0.207945% / 0.249399% / 0.277901%. Every figure is below kMaxAdmissibleBaselineNsPerBlock = 35555.6, so the 'SC-007 OVER BUDGET' WARN at harmonic_cloud_perf_test.cpp:401-407 did NOT fire in any run (previously it fired at 37384-45015 ns). Every figure is also inside the roadmap's 0.5% headline and inside the relative gates (24000x1.5 = 36000; 26000x1.5 = 39000). The binding arithmetic of spec.md:868-870 is enforced at compile time and the TU built clean: static_assert(kAutomatedBaselineNsPerBlock * kRegressionFactor <= kReferenceNsPerBlock) at :142 and static_assert(kAutomatedBaselineNsPerBlock <= kMaxAdmissibleBaselineNsPerBlock, 'SC-007: a baseline above kMaxAdmissibleBaselineNsPerBlock means the phase is over budget - reduce cost, never raise the baseline') at :149. Configuration guards also compile-enforced at :158-163 (8 chunks/block, 64 partials). Quiescent early-out measured 12287 ns/block, ratio 0.414502. |
| SC-008 | pass | HarmonicCloud_NoAllocInProcess — "All tests passed (6 assertions in 1 test case)". The mandatory liveness assertion runs FIRST (harmonic_cloud_test.cpp:4704-4720): a real `new int[16]` inside a tracked scope with `REQUIRE(livenessCount >= 1)` — its passing proves allocation_operator_overrides.h is linked into dsp_systems_tests, so the zero clause is not vacuous. That TU does not re-include the overrides header. Then `REQUIRE(renderCount == 0)` (:4789) over 200 × 512-sample blocks with all five macros automated per block and both drift banks live, plus non-vacuity `REQUIRE(renderRms > 0.0)` (:4782) and `REQUIRE(cloud.stateFinite())`. |
| SC-009 | pass | HarmonicCloud_SeededRenderIsReproducible — "All tests passed (23 assertions in 1 test case)". Configuration pinned by static_assert at ≥50 % of range for all three seed-carrying controls (harmonic_cloud_test.cpp:4845-4851: drift 40 of 50 cents, Mutation 0.75, spread 0.8). Comparison via TestUtils::compareFingerprints — no bit-exact digest. Positive clause `withinTolerance()`; negative clause `kDifferentSeedMinMetricError = 10.0 * TestUtils::kMetricTolerance` (:4854). Non-vacuity RMS > 0 asserted for both seeds (:4922-4925). The reset() clause is HarmonicCloud_ResetReproducesSeededState (passed, 4 assertions) and the 128-lane distinctness clause is HarmonicCloud_SeedDerivationIsDistinct (passed, 33792 assertions). |
| SC-010 | pass | HarmonicCloud_SampleRateInvariant — "All tests passed (672 assertions in 1 test case)". Rates {44100, 48000, 96000} (harmonic_cloud_spectral_test.cpp:670). Amplitude comparison scoped to partials below kSc010AmplitudeScopeHz = kAntiAliasFadeStart × 0.5 × 44100 = 17640 Hz (:687-688) with the exclusion asserted, and f0 = 110 Hz so the top partial is 7.04 kHz. Thresholds: `REQUIRE(std::abs(deltaDb) <= 0.5f)` (:1303) and frequency within kSc010FrequencyToleranceCents = 0.1 (:693). Unclipped precondition `REQUIRE(spectra[r].peak < 0.9f * kOutputClamp)` (:1231). |
| SC-011 | pass | HarmonicCloud_NoAliasingAtExtremes — "All tests passed (16 assertions in 1 test case)". Worst case as specified: kSc011F0Hz = HarmonicCloud::kMaxFundamentalHz = 4000 Hz (harmonic_cloud_spectral_test.cpp:407) at 44.1 kHz, 64 partials, max inharmonicity, max \|gravity\|. Alias bins are computed from the cloud's OWN partial frequencies via getPartialFrequencyHz + TestUtils::calculateAliasedFrequency + frequencyToBin + detail::sumBinPower (:545-589), with legitimate sub-Nyquist partial bins excluded (:553-560); getAliasedBins/AliasingTestConfig are explicitly not used (documented :443-451). Threshold `REQUIRE(m.aliasDb - m.fundamentalDb <= -60.0f)` (:1190), with `REQUIRE(m.subNyquistCount >= 1u)` non-vacuity (:1183). |
| SC-012 | pass | HarmonicCloud_EqualPowerPanAndSpread — "All tests passed (1881 assertions in 1 test case)". Grid section (harmonic_cloud_test.cpp:860-923) drives the shipped setPartialPosition/getPartialPanLeft/Right path (no re-implemented pan law) over {-1,-0.5,0,+0.5,+1} for all 64 partials: clause 1 \|L²+R²-1\| ≤ 1e-6, clause 2 L ≥ 0 and R ≥ 0, clause 3 strict L-decreasing / R-increasing, clause 4 \|L-R\| ≤ 1e-6 at centre. Spread section (:939-1041): `REQUIRE(worstCentreDelta <= 1.0e-7)` at spread 0 over the full render, and strictly decreasing inter-channel correlation across 5 spreads. |
| SC-013 | pass | HarmonicCloud_PartialEnvelopeOffsetsStagger — "All tests passed (1417 assertions in 1 test case)". Sampled via getPartialCurrentAmplitude once per block with Mutation and drift at 0; crossing = 50 % of that partial's steady-state target. Clause 1 `REQUIRE(flatMax - flatMin <= std::size_t{1})` (≤1 block at spread 0); clause 2 `REQUIRE(staggerSeconds >= 0.100)` and `<= kMaxEnvOffsetSec = 2.0` (harmonic_cloud_test.cpp:2706-2707, :2788-2789); clause 3 `REQUIRE(fraction >= 0.95)` within attack + that partial's own offset (:2710, :2807) using the per-partial getPartialAttackOffsetSec accessor; clause 4 `REQUIRE(worstRise <= 0.0)` (monotone non-increasing), `REQUIRE(fraction <= 0.01)` within decay + offset + 5τ (:2713, :2857), and `REQUIRE(halfFraction >= 0.30)` at half decay so no partial is cut off early (:2721, :2846). |
| SC-014 | pass | HarmonicCloud_RichnessAddsPartialsAndEnergy — "All tests passed (452 assertions in 1 test case)". f0 = 110 Hz, 48 kHz, 65536-point Blackman-Harris FFT, settings {0, 0.25, 0.5, 0.75, 1.0}. `REQUIRE(m.activeCount == kExpectedCount[s])` with {1,3,8,23,64} (harmonic_cloud_spectral_test.cpp:972, :991) AND `REQUIRE(m.countAboveFloor == kExpectedCount[s])` (:1027) at the -60 dB audibility floor; per-partial `REQUIRE(\|relativeDb - (-20 p log10 n)\| <= 0.5f)` (:1011); weakest active partial within 0.6 dB of the spec table (:1021). Monotone non-decreasing in both metrics (:1035-1036); endpoints `countAtOne - countAtZero >= 16` (:1043) and `fractionAtOne - fractionAtZero >= 20.0f` (:1048). |
| SC-015 | pass | HarmonicCloud_DriftIsIndependentDecimatedAndBounded — "All tests passed (295 assertions in 1 test case)". Clause 1: kMeanCorrelationLimit = 0.2, kPairCorrelationLimit = 0.5 over all 2016 pairs (harmonic_cloud_test.cpp:1491-1492). Clause 2: reads == Σceil(blockSize/64) asserted for 1×N, 512- and 577-sample schedules with explicit negative assertions that it is NOT one-per-block (:1605-1761), lane state agreement within kFinalValueTolerance = 1e-5 (:1613), and render fingerprint match between the single-block and 512-block schedules. Clause 3: bound ≤ depth + 0.1 cent, liveness ≥ 0.25 × depth (kLivenessFraction = 0.25, :1772), index scaling `highMean >= 4.0 * lowMean` (kIndexScalingFactor = 4.0, :1778), scatter `inversions >= 1`. Clause 4: after one cloud-level setter call all 64 detune lanes respect the new bound and `worstMutationDelta == 0.0f`. Additionally HarmonicCloud_DriftLaneMatchesBrownianDrift passed with 270012 assertions, proving the SoA lane IS BrownianDrift to 1e-5 over 60 s on both banks at smoothness {0, 0.5, 1}. |
| SC-016 | pass | HarmonicCloud_MutationStaysBoundedAndLevelStable — "All tests passed (676 assertions in 1 test case)". Weight measured as targetAmplitude ÷ unmutatedTargetAmplitude after the attack has elapsed. Bounds kMinLegalWeight = 0.25 / kMaxLegalWeight = 1.75 (with 2·FLT_EPSILON relative measurement slack); 1 s-window RMS `REQUIRE(std::abs(worstDb) <= 3.0)`; rate `REQUIRE(run.worstBlockDeltaWeight <= 10.0 * blockDuration)`; exact 1.0 at Mutation 0 (`REQUIRE(run.minWeight[i] == 1.0)` and `maxWeight[i] == 1.0`, bit-exact, not a tolerance). Independence config 1 (drift 0, Mutation 1, harmonic_cloud_test.cpp:2453): weights still move for ≥ half the active partials (`REQUIRE(2 * moving >= full.activeCount)`) with detune exactly 0. Config 2 (drift max, Mutation 0, :2505): every weight exactly 1.0 while `REQUIRE(run.maxAbsDetuneCents > 1.0)`. Per-partial weight-vs-detune correlation `REQUIRE(worstAbsR <= 0.3)`. |
| SC-017 | pass | HarmonicCloud_ParameterGridStaysFiniteAndBounded — "All tests passed (11951 assertions in 1 test case)". Pass 1 is the FULL Cartesian grid: `static_assert(kSc017CellCount == 3888)` from 3^5 macros × 2^4 endpoint axes (harmonic_cloud_test.cpp:3310-3311), each rendered 1 s at 48 kHz through a note-on, with every sample scanned by bit-pattern detail::isNaN/isInf (:3375-3381) and `REQUIRE(scan.worstMagnitude <= kOutputClamp)` (:3470) plus stateFinite() per cell. Pass 2 (:3478) feeds NaN 0x7FC00000 / +Inf 0x7F800000 / -Inf 0xFF800000 built through a volatile sink (:3323-3328, `sc017FromBits`) — no std::numeric_limits — to every setter including setPartialPosition, asserting the getter is exactly unchanged and the subsequent render matches the pre-call render_fingerprint. Pass 3 (:3573) covers finite out-of-range clamping. The {0 drift depth} × {max Mutation} cells are present in the enumeration (kSc017DriftGrid{0, kMaxDriftCents} × kSc017MutationGrid{0, 0.5, 1}). |
| SC-018 | pass | HarmonicCloud_OnsetIsPhaseIncoherent — "All tests passed (43 assertions in 1 test case)". 64 partials at Richness 1.0, maximum tilt, 8 distinct seeds (kSc018Seeds, harmonic_cloud_test.cpp:~716). `REQUIRE(onsetPeak <= 0.9f * HarmonicCloud::kOutputClamp)` over the first 100 ms (4800 samples, :800); `REQUIRE(onsetCrestDb <= steadyCrestDb + 6.0f)` against the 2.0-2.1 s steady window of the same render; seeded-ness `REQUIRE(stddev > 0.0)` and `REQUIRE(maxPeak - minPeak >= 0.10f * mean)` across the 8 seeds, so a fixed onset peak fails. |

## Compliance table — success criteria, independent SC-lens re-measurement

| ID | Verdict | Evidence (verbatim) |
|---|---|---|
| SC-001 | pass | dsp/tests/unit/systems/harmonic_cloud_test.cpp:253 (assert :311). Ran dsp_systems_tests.exe "HarmonicCloud_PartialFrequencyAccuracyWithin0p1Cent" --success. All 10 pinned (f0,n) pairs measured; per-partial cents error: 0, -0.00010319, 0, -0.00010319, -0.00010319, -0.00010319, -0.00010319 (14080 Hz), 0 (1000 Hz), 0 (8000 Hz), 0.000206379 (16000 Hz). Max \|error\| = 2.06e-4 cent vs threshold < 0.1 cent. The 0.8*Nyquist constraint is asserted per case (e.g. "14080.0 < 19200.0"). Estimator resolution documented by HarmonicCloud_FrequencyEstimatorResolution (:139): 0 cent error at 55/440/3520/7040 Hz against < 0.001 cent, i.e. >=100x finer than 0.1 cent. 'All tests passed'. |
| SC-002 | pass | harmonic_cloud_test.cpp:322 (assert :374). Ran the case with --success at f0=110 Hz, B in {0.01, 0.05, 0.1} (3 non-zero values, >=3 required), 16 partial measurements total (6/5/5). Measured cents errors, all values seen: 0, -0.00010319, 0.000206379. Max \|error\| = 2.06e-4 cent vs the stated thresholds (<= 1 cent AND <= the documented estimator tolerance of 0.001 cent) — both satisfied. Non-vacuity asserted (measured > 0 per B). 'All tests passed'. |
| SC-003 | pass | dsp/tests/unit/systems/harmonic_cloud_spectral_test.cpp:1077 (asserts :1107, :1117, :1137); config pinned at :231-278 (f0=110 Hz, Richness 5/6 -> N=32, 48 kHz, 4 s, FFT 65536, Blackman-Harris — matches spec). Ran with --success: fitted slopes -17.5396 / -11.5396 / -5.53959 / 0.4604 / 6.46041 dB/oct; differential (slope(t) - slope(0)) = -12, -6, 0, 5.99999, 12 vs settings -12/-6/0/+6/+12 -> max error 1e-5 dB/oct against 0.5 dB/oct. Tilt-0 fitted slope -5.53959 vs -5.52 expected -> error 0.0196 dB/oct <= 0.5. Unclipped precondition peaks 0.538953 / 0.58651 / 0.781374 / 0.849369 / 0.908514 all < 1.8. 346 assertions, all passed. |
| SC-004 | pass | harmonic_cloud_test.cpp:449. Ran with --success (201 assertions, all passed). Clause 1 (g=0): grid deviations 0 / -0.00010319 cent for n in {1,2,4,8,16,32}, all < 0.1 cent tolerance. Clause 2: mean \|deviation\| over n>=2 at g = -1, -0.5, 0, +0.5, +1 = 360, 180, 6.19138e-05, 180, 360 cent; every step exceeds the required 5 x 0.1 = 0.5 cent by ~360x. Clause 3: 5 of 5 partials with n>=2 qualified and each asserted devPlus>0, devMinus<0, devPlus*devMinus<0 (:635-637). Clause 4 (composition order, B=0.05, g=+/-1, n in {1,2,4,8,16}): law-error values 0.000206379 cent worst (e.g. n=16 g=+1 predicts 8627.09 Hz, measured 8627.09 Hz) vs <= 1 cent. A presence guard (amplitude at the predicted frequency >= 0.5 x render peak, :544) blocks the wrapped-estimator false pass. |
| SC-005 | pass | Previously-failing cause REMOVED: the spec IS now amended. specs/seraphis-phase2-harmonic-cloud/spec.md:727 carries a blockquote '> **AMENDED 2026-07-26 (implementation measurement).** The pass condition below replaces an earlier one that a correct implementation cannot satisfy. The withdrawn wording was: 1. detections(modulated) <= detections(control), and 2. maxPerSampleDelta(modulated) <= 1.5 x maxPerSampleDelta(control)', and spec.md:758 states the replacement '*Pass condition (differential, as amended).*' with clause 1 at :762 ('No control-grid signature ... must not exceed 2.25x the uniform expectation') and clause 2 at :767 ('Matched-regime slew ... slowControl ... drift smoothness set to 1.0'). The earlier verdict rested solely on 'the criterion was changed in test code only, the spec was never amended' — that is no longer true; both amended clauses, both measured figures, and both mandatory positive controls are written into spec.md:762-777. RAN THIS SESSION: build/windows-x64-release/bin/Release/dsp_systems_tests.exe "HarmonicCloud_NoZipperUnderMutationAndDrift" --success -> 'All tests passed (24 assertions in 1 test case)', exit 0. Measured values match the amended spec text verbatim: clause 1 'left channel: 6 of 126 detections on the 64-sample control grid (4.7619%), uniform = 6.25%, limit = 14.0625%' and 'right channel: 5 of 141 detections on the control grid (3.5461%), limit = 14.0625%' (asserts at harmonic_cloud_test.cpp:4256 and :4268); clause 2 'left channel: max \|dx\| fast drift = 0.305248, slow drift = 0.278125, bound = 0.417187' (ratio 1.0975) and 'right channel: max \|dx\| fast drift = 0.32557, slow drift = 0.298882, bound = 0.448324' (ratio 1.0893), asserts at harmonic_cloud_test.cpp:4317 and :4324. Both mandatory positive controls fire: 'positive control: injected a 0.341992 step (2 x the control's own max \|dx\|) at sample 720000; 1 detection(s)' (REQUIRE(detectionsAtInjection >= 1), :4130) and 'synthetic zipper positive control: 166 detections, 166 on-grid' (REQUIRE >= 32, REQUIRE >= 0.9, :4282/:4285). The three withdrawn-clause guards demanded by spec.md:753-755 ('pinned by a standing assertion ... phrased as the negation of the withdrawn clause') are all present and all fired: 'frozen control scores 0 detections (both channels), the modulated render 267' (REQUIRE(controlDetections < modulatedDetections), :4205), 'max \|dx\| ratio against the FROZEN control = 1.78512' (REQUIRE(withdrawnRatioL > 1.5), :4218), and 'SC-005's literal 10%-of-peak injection is 0.0747897' vs max \|dx\| 0.170996 (REQUIRE(specInjectStep < controlDeltaForInjection), :4157). |
| SC-006 | pass | Previously-failing cause REMOVED: spec.md:795 now carries '> **AMENDED 2026-07-26 (implementation measurement).** The withdrawn threshold was **detections(swept) <= detections(control)** ... It is not a criterion, because its verdict is decided by a choice the wording never makes', and spec.md:812-816 states the replacement: 'Threshold: **zero detections on the 512-sample block grid** (+/-1 sample) for each of the five macro sweeps, on both channels, and for the fundamental step.' RAN THIS SESSION: dsp_systems_tests.exe "HarmonicCloud_MacroSweepsAreClickFree" --success -> 'All tests passed (28 assertions in 1 test case)', exit 0. All twelve on-grid assertions (6 renders x 2 channels, REQUIRE(onGrid == 0) at harmonic_cloud_test.cpp:4525) measured ZERO on-grid: totals per channel were 0, 1, 2, 5, 8 and 13 across the Richness / Inharmonicity / Tilt / Mutation / Gravity sweeps, and 'fundamental step 220 -> 440 Hz / left channel: 0 of 1 detections on the 512-sample block grid' + 'right channel: 0 of 1' — exactly the '0 on-grid out of totals of 0-13 per channel' spec.md:816-817 records. Both mandatory positive controls fire, including the literal SC-005 peak form that SC-005 itself could not use: 'positive control: injected a 0.1797 step (2 x the control's own max \|dx\|) at sample 120064; 1 detection(s)', 'SC-006 positive control (literal SC-005 form): injected a 0.0828493 step (10% of the control's peak 0.828493) ... 1 detection(s)' (:4450), and 'synthetic per-block zipper: 22 detections, 22 on the block grid' (REQUIRE >= 16 and >= 90% on-grid, :4469/:4470). The withdrawn-clause guard stands and fired: 'a STATIC render at the Richness sweep's min endpoint scores 0 detections and at its max endpoint 43 - both are the frozen control of the same configuration', REQUIRE(endpointSpread >= 16) at :4509. Retrigger clause — the one clause SC-006 KEPT as a count comparison (spec.md:820-830) — verified separately: dsp_systems_tests.exe "HarmonicCloud_RetriggerIsClickFree" -> 'All tests passed (1428 assertions in 1 test case)', measuring 'left channel: retriggered = 3 detections, control = 3' and 'right channel: retriggered = 4 detections, control = 4' (meets detections(retriggered) <= detections(control)), plus the mechanism: 'MCF states changed across a sounding retrigger: 0 of 64 (must be 0)' and the non-vacuity case 'MCF states changed across a quiescent note-on: 64 of 64' after 'reached quiescence after 11 blocks (cap 60)'. |
| SC-007 | pass | BOTH previously-violated clauses are now satisfied, and the fix was cost reduction, not baseline inflation. (1) BASELINE PROVENANCE — the 'PROVISIONAL - NOT YET MEASURED ON THIS MACHINE' labels are gone. harmonic_cloud_perf_test.cpp:100-119 is a 'BASELINE PROVENANCE (SC-007, spec.md "Baseline provenance")' block recording exactly what spec.md:874-878 demands: 'Machine : Windows 11 Pro 26200, 13th Gen Intel(R) Core(TM) i9-13900HX / Build : MSVC Release, build/windows-x64-release / Trial shape: best-of-25 x 500 blocks / Measured : 2026-07-26', followed by an eight-run table (static min 20641 max 23154; automated min 21917 max 25262; quiescent min 9166 max 10998). kStaticBaselineNsPerBlock = 24000.0 (:123) and kAutomatedBaselineNsPerBlock = 26000.0 (:140) are the worst of those figures rounded up. (2) OVER-BUDGET — resolved. Cost was cut via three levers documented at :86-98 with measurements (31281-32027 -> 20641-23154 static; 33257-34184 -> 21917-25262 automated): detail::centsToDriftRatio (harmonic_cloud.h:106-112, a bounded-domain degree-4 Horner replacing std::pow, 512 calls/block), detail::kHarmonicCloudLog2N, and DriftLanes::cachedPowN (harmonic_cloud.h:1620-1628). :137-139 states the discipline was followed: 'the first honest measurement of this component was 35,052 static / 37,002 automated, over the 35,556 ceiling.' RAN THIS SESSION, three separate invocations of dsp_systems_tests.exe "HarmonicCloud_CpuBudget*" (each 'All tests passed (10 assertions in 2 test cases)'): static 20114.6 / 25116.4 / 27628.4 ns per 512-block = 0.188574% / 0.235466% / 0.259016% of budget; automated (GATING) 22180.8 / 26602.6 / 29642.8 ns = 0.207945% / 0.249399% / 0.277901%. Every figure is below kMaxAdmissibleBaselineNsPerBlock = 35555.6, so the 'SC-007 OVER BUDGET' WARN at harmonic_cloud_perf_test.cpp:401-407 did NOT fire in any run (previously it fired at 37384-45015 ns). Every figure is also inside the roadmap's 0.5% headline and inside the relative gates (24000x1.5 = 36000; 26000x1.5 = 39000). The binding arithmetic of spec.md:868-870 is enforced at compile time and the TU built clean: static_assert(kAutomatedBaselineNsPerBlock * kRegressionFactor <= kReferenceNsPerBlock) at :142 and static_assert(kAutomatedBaselineNsPerBlock <= kMaxAdmissibleBaselineNsPerBlock, 'SC-007: a baseline above kMaxAdmissibleBaselineNsPerBlock means the phase is over budget - reduce cost, never raise the baseline') at :149. Configuration guards also compile-enforced at :158-163 (8 chunks/block, 64 partials). Quiescent early-out measured 12287 ns/block, ratio 0.414502. |
| SC-008 | pass | harmonic_cloud_test.cpp:4694. Ran with --success. Liveness assertion first: 'liveness probe: observed = 42, counted allocations = 1' (>= 1 required), proving allocation_operator_overrides.h is linked into dsp_systems_tests via selectable_oscillator_test.cpp and the new TU correctly does not re-include it. Measurement: 'heap allocations across 200 blocks of 512 with macro automation and both drift banks live = 0' vs threshold 0. Non-vacuity: render RMS over 200 automated blocks = 0.184591 > 0, and cloud.stateFinite() asserted. 6 assertions passed. |
| SC-009 | pass | harmonic_cloud_test.cpp:4831. Configuration pinned by static_assert at :4845-4850 (driftCents >= 0.5 x kMaxDriftCents, mutation >= 0.5, spread >= 0.5), so the negative control cannot pass vacuously. Ran with --success: same seed -> 'worst metric relative error = 0, worst sample error = 0' on both channels, within kMetricTolerance = 1e-5 / kSampleTolerance = 1e-4. Different seed -> worst metric relative error 0.118153 (L, checkpoint[1]) and 0.131475 (R, checkpoint[10]) against the required > 10 x kMetricTolerance = 1e-4 (:4854) — exceeded by ~1200x. Uses render_fingerprint.h, not a bit-exact digest. 23 assertions passed. |
| SC-010 | pass | harmonic_cloud_spectral_test.cpp:1215. Ran with --success (672 assertions, all passed). Frequency: every cross-rate difference reported as 'difference 0 cent' vs the SC-001 tolerance of 0.1 cent. Amplitude: worst per-partial cross-rate difference = -1.48773e-04 dB (extremes of the sorted set: -0.000148773 to +0.000114441 dB) vs the 0.5 dB threshold. Scoping is asserted, not assumed: every measured partial frequency is REQUIREd below 0.8 x Nyquist at 44.1 kHz (expansions '110.0 < 17640.0' ... '1980.0 < 17640.0', config f0=110 Hz x 64 partials -> 7040 Hz top). Envelope-crossing agreement also checked (0.0269388 s at 44.1 kHz vs 0.027 s at 48/96 kHz, difference 6.12e-05 s vs a 0.00145125 s one-chunk tolerance). |
| SC-011 | pass | harmonic_cloud_spectral_test.cpp:1161; config :397-432 and :479-488 — 44.1 kHz (lowest supported), f0 = kMaxFundamentalHz = 4000 Hz, Richness 1.0 (64 active, asserted), B = kMaxInharmonicity = 0.1, \|g\| = 1 at BOTH signs. Ran with --success: gravity +1 -> alias -59.4507 dB, fundamental 67.8286 dB, ratio -127.279 dB; gravity -1 -> alias -54.4445 dB, ratio -122.273 dB. Threshold <= -60 dB, so 62-67 dB of margin. Non-vacuity asserted: subNyquistCount = 3 (>=1 and < 64) and aliasBinCount = 61 > 0 after the +/-2-bin exclusion around every legitimate partial; unclipped precondition peaks 0.559766 / 0.587101 < 2.0. getAliasedBins/AliasingTestConfig are not used, per spec. 16 assertions passed. |
| SC-012 | pass | harmonic_cloud_test.cpp:857. Ran with --success. Measured on the shipped conversion path via setPartialPosition + getPartialPanLeft/Right (no re-implementation of the pan law). Grid {-1,-0.5,0,+0.5,+1} x 64 partials: clause 1 \|L^2+R^2-1\| <= 1e-6 (asserted 320x); clause 2 L,R >= 0 everywhere — measured (L,R) = (1,0) at -1, (0.707107,0.707107) at 0, (0,1) at +1, i.e. no polarity inversion; clause 3 L strictly decreasing / R strictly increasing asserted 512x; clause 4 \|L-R\| at position 0 = 0 <= 1e-6. Spread: worst \|L-R\| over the full spread-0 render = 0 <= 1e-7; inter-channel correlation across 5 settings (>=4 required) = 1, 0.981154, 0.925913, 0.838052, 0.723498 — strictly decreasing. |
| SC-013 | pass | harmonic_cloud_test.cpp:2698 (measurement harness :2612). Ran with --success, 1417 assertions passed. Clause 1 (offset spread 0): crossing blocks span [2,2] = 0 s spread, threshold <= 1 block. Clause 2 (spread 1): span [2,94] = 0.981333 s, thresholds >= 0.100 s and <= kMaxEnvOffsetSec = 2.0 s. Clause 3: worst per-partial fraction of steady-state target reached by its own attack+offset deadline = 0.992024 vs the 0.95 bar (checked at both spread settings, all 64 partials). Clause 4: worst rise after gate-off asserted <= 0 for every partial (monotone non-increasing); half-decay level >= 0.30 asserted; residual at decay+offset+5*tau = 9.19895e-06 worst, vs the 0.01 bar. Reference target is unmutatedTarget x antiAliasGain (:2650), not the env-moving target. |
| SC-014 | pass | harmonic_cloud_spectral_test.cpp:967. Ran with --success, 452 assertions passed. Active count at Richness {0,0.25,0.5,0.75,1.0} = 1, 3, 8, 23, 64 — exactly round(64^r), asserted (:992), and the measured count above the -60 dB audibility floor equals the same numbers (:1027). Per-partial rolloff asserted against n^(-p(r)) within 0.5 dB — measured vs expected agree to 4-5 significant figures (e.g. n=19: -12.7876 vs -12.7875 dB; n=10: -22.5 vs -22.5 dB). Energy fraction above the fundamental = -80, -13.8034, -9.47285, -5.06505, -1.02663 dB, monotonically non-decreasing. Endpoints: count(r=1)-count(r=0) = 64-1 = 63 >= 16; energyFractionDb(r=1)-(r=0) = -1.02663-(-80) = 78.97 dB >= 20 dB. Unclipped precondition asserted per setting. |
| SC-015 | pass | harmonic_cloud_test.cpp:1469. Ran with --success, 295 assertions passed. Clause 1: mean pairwise \|r\| over 2016 pairs = 0.052035 (<= 0.2), worst pair (26,52) = 0.228472 (<= 0.5), 60 s at 48 kHz. Clause 2: drift reads per partial = single-block 4616, 512-block 4616, 577-block 5120 (= 10 per 577-sample block), each REQUIREd equal to sum(ceil(blockSize/64)) and explicitly REQUIREd != one-per-block; per-partial detune agreement across schedules 'worst detune-multiplier spread = 0' (<= 1e-5); rendered single-block vs 512-block agree at 'worst metric relative error = 0, worst sample error = 0' under render_fingerprint tolerances. Clause 3: largest \|detune\| = 45.401 cent (partial 63) vs configured depth 50 cent (bound) and >= 0.25 x 50 = 12.5 cent (liveness); at depth 0 the lane still walks (excursion 0.999561) while 'worst \|multiplier - 1\| = 0'; index scaling mean\|detune\| n in [33,64] = 10.4815 cent vs n in [1,8] = 0.931084 cent -> ratio 11.26 >= 4; 27 inversions in the per-partial mean sequence (>= 1 required). Clause 4: post-setter bound re-asserted for all 64 partials. |
| SC-016 | pass | harmonic_cloud_test.cpp:2280. Ran with --success, 676 assertions passed; 60 s renders at Mutation 0/0.5/1.0, sampling starts after the attack so env_i = 1. Weight extrema within [0.25, 1.75] (+/- 2 float ULP measurement slack, :2299-2302) — e.g. Mutation 0.5 ranges [0.625, 1.375] = 1 +/- 0.5*0.75 exactly; Mutation 0 gives [1,1] on every partial and worstBlockDeltaWeight == 0.0. Level: worst 1 s window level deviation = 1.53983 dB (window 23 of 59) vs the +/-3 dB bound, reference Mutation-0 RMS = 0.356654. Rate: worst per-block \|dw\| = 0.0262263 vs kMaxBlockDeltaWeight = 10 s^-1 x 512/48000 = 0.10667. Independence 1 (drift depth 0, Mutation 1.0): 64 of 64 partials strayed >= 0.1 from unity (>= half required), worst window 1.41891 dB. Independence 2 (drift max, Mutation 0): every weight exactly 1.0 while largest \|detune\| = 45.6223 cent. Weight-vs-own-detune Pearson: mean \|r\| = 0.0697878, worst \|r\| = 0.245968 (partial 19) vs <= 0.3. |
| SC-017 | pass | harmonic_cloud_test.cpp:3403. Ran the case: 'All tests passed (11951 assertions in 1 test case)'. Grid is the full Cartesian enumeration, kSc017CellCount = 3*3*3*3*3*2*2*2*2 = 3888 with a static_assert against spec.md:979-981 (:3316-3317), each cell rendered kSc017GridSamples = 48000 samples = 1 s at 48 kHz. Per cell: scan.allFinite by bit-pattern (no std::isnan), worstMagnitude <= kOutputClamp = 2.0f, and cloud.stateFinite() at the end. Setter hygiene covers all 13 setters including setPartialPosition (:3538-3562 list): setFundamentalHz, setRichness, setInharmonicity, setSpectralTiltDb, setMutation, setSpectralGravity, setDriftDepthCents, setDriftSmoothness, setStereoSpread, setAttackTimeSec, setDecayTimeSec, setEnvelopeOffsetSpread, setPartialPosition — each fed NaN/+Inf/-Inf built from bit patterns and verified via std::bit_cast before use, then getter asserted bit-identical (==, not a tolerance) and the post-call render compared to the pre-call render through render_fingerprint. Clamp endpoints separately asserted (20 Hz / 4000 Hz, 0/1 richness, +/-12 dB/oct, 0/50 cents, etc.). |
| SC-018 | pass | harmonic_cloud_test.cpp:708. Ran with --success. 8 distinct seeds (>= 8 required), 64 partials at Richness 1.0, tilt +12 dB/oct, f0 = 110 Hz. Clause 1: onset peaks in the first 100 ms = 1.00742, 1.03014, 1.07743, 1.10927, 1.1272, 1.27611, 1.31292, 1.319 — max 1.319 <= 0.9 x kOutputClamp = 1.8 (2.70 dB margin). Clause 2: onset peak-to-RMS vs steady peak-to-RMS pairs = 10.909/9.03422, 11.1881/9.34254, 11.6856/9.86454, 11.7499/9.93257, 11.8784/10.0842, 12.3897/10.5181, 13.1681/11.3416, 13.2594/11.4075 dB -> worst excess ~1.87 dB against the 6 dB allowance. Clause 3: onset peaks min 1.00742, max 1.319, mean 1.15744, stddev 0.118693; max-min = 0.3116 >= 0.10 x mean = 0.1157, so the peak genuinely varies with the seed. (Note: the header comment at :687-688 claims a worst excess of +0.180 dB — stale, the live measurement is ~1.87 dB, still well inside the bar.) |

## Compliance table — cross-cutting constraints

| ID | Verdict | Evidence (verbatim) |
|---|---|---|
| CC-rt | pass | The alloc/lock/exception half was already clean and remains so after the SC-007 optimisation; the CPU-budget half — the sole reason for the prior 'partial' — is now resolved. RT-SAFETY RE-SWEPT ON THE CHANGED HEADER (now 1775 lines, up from 1639): grep -nE 'new \|delete \|malloc\|std::vector\|std::string\|std::function\|mutex\|lock\|throw\|try \{\|printf\|fopen\|std::cout\|shared_ptr\|unique_ptr\|resize\(\|push_back\|emplace\|std::isnan\|std::isinf\|isfinite' over dsp/include/krate/dsp/systems/harmonic_cloud.h returns ZERO code hits — all 11 matches are comment/identifier text ('512-sample block' at :68/:71/:72/:941/:1498/:1609/:1611, 'processStereoBlock' at :682, 'block size' at :710-712, 'lock-free' in the RT-safety doc at :121, 'never std::isnan' at :857). The two new hot-path constructs are pure arithmetic and noexcept: detail::centsToDriftRatio (:106-112) is 'const float u = cents * kCentsToNatLog; return 1.0f + u * (1.0f + u * (0.5f + u * (1.0f/6.0f + u * (1.0f/24.0f))));' — four FMAs, no library call; advanceSmootherAllLanes (:1620-1641) is 'void advanceSmootherAllLanes(DriftLanes& bank, int numSamples) noexcept' operating on fixed-capacity bank arrays, calling std::pow only when bank.cachedPowN != numSamples (:1626-1629). MEASURED: dsp_systems_tests.exe "HarmonicCloud_NoAllocInProcess" --success -> 'All tests passed (6 assertions in 1 test case)' with the mandatory liveness probe proving the detector is wired, not vacuous — 'liveness probe: observed = 42, counted allocations = 1', REQUIRE(livenessCount >= 1) PASSED — then REQUIRE(renderCount == 0) PASSED across the automated render loop. CPU BUDGET NOW MET, WHICH CLOSES THE PARTIAL: the cross-cutting rule 'CPU budgets are functional requirements' is satisfied by measurement — three runs of the gating automated case this session read 22180.8 / 26602.6 / 29642.8 ns/block (0.208% / 0.249% / 0.278% of one core), all below the 35555.6 ns admissible ceiling, so the 'SC-007 OVER BUDGET' warning that previously accompanied a green suite no longer appears in any run. The specific root cause named in the prior finding — updateControl re-running 64x std::pow/std::sqrt/std::sin recalculateFrequencies and 64x std::pow x2 + std::log2 recalculateAmplitudes per chunk — was addressed by the detail::kHarmonicCloudLog2N and centsToDriftRatio levers documented at harmonic_cloud_perf_test.cpp:86-98. PORTABILITY: node tools/check-portability.js -> 'check-portability: all clear -- 23 compiled.' FULL SUITES GREEN, all five run by me this session: dsp_core_tests 'All tests passed (1584873 assertions in 570 test cases)'; dsp_primitives_tests 'All tests passed (4549254 assertions in 1504 test cases)'; dsp_processors_tests 'All tests passed (10620124 assertions in 3278 test cases)'; dsp_systems_tests 'All tests passed (5999345 assertions in 1093 test cases)'; dsp_effects_tests 'All tests passed (93062 assertions in 455 test cases)'. Including the perf cases: dsp_systems_tests.exe "HarmonicCloud*" -> 'All tests passed (326295 assertions in 28 test cases)'. Build of all five targets produced zero errors and zero warnings. |
| CC-layers | pass | New header lives at dsp/include/krate/dsp/systems/harmonic_cloud.h = Layer 3, self-declared at :4 ('Layer 3: Systems - HarmonicCloud') and :47 ('@par Layer: 3 (systems/). Dependencies: Layer 0/1/2 + stdlib only'). Its complete include list (:17-29) points strictly downward: core/crossfade_utils.h, core/db_utils.h, core/math_constants.h, core/pitch_utils.h, core/random.h (5x Layer 0), primitives/smoother.h (Layer 1), processors/harmonic_oscillator_bank_simd.h (Layer 2 -- self-declared 'Layer 2: SIMD-Accelerated Harmonic Oscillator Bank Kernels' at harmonic_oscillator_bank_simd.h:4, and 'Principle IX: Layer 2 (depends on Layer 0)' at :16), plus <algorithm> <array> <cmath> <cstddef> <cstdint>. All 7 krate headers confirmed present at those exact paths via ls. No sideways (systems->systems) and no upward (systems->effects) include. Reverse-dependency check: grep -rln 'harmonic_cloud.h' dsp/ plugins/ --include=*.cpp returns ONLY the 3 test TUs, so no lower layer pulls it in. Includes are unconditional -- node tools/lint-arch-guarded-includes.js -> 'lint-arch-guarded-includes: OK -- no krate includes behind architecture guards.' (exit 0). Tests mirror the layer tree at dsp/tests/unit/systems/ and are registered in the matching target: git diff dsp/tests/CMakeLists.txt shows the 3 files added inside add_executable(dsp_systems_tests ...) at lines 329+ (harmonic_cloud_test.cpp, harmonic_cloud_spectral_test.cpp, harmonic_cloud_perf_test.cpp), so none silently drops. ODR sweep clean: grep -rn 'class HarmonicCloud\|struct HarmonicCloud\|struct LaneRng\|class LaneRng\|struct DriftLanes\|class DriftLanes' dsp/ plugins/ returns exactly 3 hits, all in harmonic_cloud.h (:49, :849, :853). The known kMaxPartials collision is correctly dodged: namespace-scope Krate::DSP::kMaxPartials = 96 at processors/harmonic_types.h:21 vs class-scoped HarmonicCloud::kMaxPartials = 64 at harmonic_cloud.h:60 -- and harmonic_cloud_test.cpp includes BOTH headers in one TU (:11 and :18) and compiles, proving the dodge holds. |
| CC-naming | pass | Classes PascalCase: HarmonicCloud (harmonic_cloud.h:49), LaneRng (:849), DriftLanes (:853). Namespace PascalCase: 'namespace Krate::DSP' (:39). Methods camelCase throughout -- prepare/reset (:179,:210), setFundamentalHz/getFundamentalHz (:265,:287), setRichness/setInharmonicity/setSpectralTiltDb/setMutation/setSpectralGravity (:294,:308,:321,:334,:360), processStereoBlock (:606), deriveSeed (:575), redrawPhases (:880), reseed (:899), applyEnvelopeOffsetSpread (:951), recalculateFrequencies (:974), tiltGain (:999), recalculateAmplitudes (:1027), retireFadedTail (:1070), recalculatePan (:1084), recalculateAntiAliasing (:1107), updateAntiAliasGain (:1118), advanceEnvelope (:1166), updateControl (:1199), currentNormGainTarget (:1342), updatePanGains (:1363), updateDriftCoefficients (:1408), advanceControlStepAllLanes (:1433), advanceSmootherAllLanes (:1490), advanceDriftLanes (:1521), resetDriftLanes (:1536). Class data members all carry the trailing underscore (:1549-1632: sinState_, cosState_, epsilon_, detuneMultiplier_, currentAmplitude_, targetAmplitude_, antiAliasGain_, panLeft_, panRight_, frequencyHz_, baseAmplitude_, unmutatedTarget_, panPosition_, positionScatter_, positionOverridden_, driftAmount_, attackOffsetSec_, decayOffsetSec_, attackOffsetDraw_, decayOffsetDraw_, envValue_, envStage_, masked_, detuneLanes_, mutationLanes_, driftSmoothCoeff_, sampleRate_, nyquist_, invSampleRate_, fadeStart_, invFadeRange_, ampSmoothCoeff_, activeCount_, kernelCount_, tailHighWater_, freqDirty_, ampDirty_, normGain_, gainSmoothed_, configRng_, phaseRng_, configuredSeed_, prepared_, gate_, lastOutL_, lastOutR_, crossfadeOldL_, crossfadeOldR_, crossfadeRemaining_, crossfadeLengthSamples_, crossfadeThresholdRatio_, driftReadCount_, fundamentalHz_, richness_, inharmonicity_, tiltDb_, mutationAmount_, gravity_, driftCents_, driftSmoothness_, stereoSpread_, attackSec_, decaySec_, offsetSpread_). The nested aggregate members (DriftLanes::walk/smoothCur/smoothTgt/rng/a/g/depth/samplesUntilControl at :854-861; LaneRng::rng at :850) carry NO trailing underscore, which MATCHES this library's precedent for public POD aggregates -- F0Estimate{frequency, confidence, voiced} and Partial{harmonicIndex, frequency, amplitude, phase} at dsp/include/krate/dsp/processors/harmonic_types.h:26-40 -- while class-with-invariants precedent (BrownianDrift private members a_, g_, smoothness_, depth_, mean_, x_, samplesUntilControl_, configuredSeed_, rng_ at brownian_drift.h:272-287) uses it, exactly as HarmonicCloud does. Constants kPascalCase: verified exhaustively by grep of every 'constexpr <type> <name> =' in the header filtered for names NOT matching ' k[A-Z]' -> ZERO non-conforming results (covers kMaxPartials, kControlChunkSamples, kDriftControlInterval, kDriftTauMin/Max, kDriftInternalStd, kDriftOutputSmoothMs, kDriftWalkLimit, kDriftDenormalFloor, kAmpSmoothTimeSec, kAntiAliasFadeStart, kMaxEpsilon, kOutputClamp, kTargetOscRms, kMaxNormGain, kNormGainSmoothMs, kMinFundamentalHz, kMaxFundamentalHz, kCrossfadeTimeSec, kMaxInharmonicity, kMinTiltDbPerOct, kMaxTiltDbPerOct, kGravityExponentRange, kRichnessMinExponent, kRichnessMaxExponent, kMaxMutationDepth, kMutationSmoothness, kDriftIndexExponent, kMaxDriftCents, kMinAttackSec, kMaxAttackSec, kMinDecaySec, kMaxDecaySec, kMaxEnvOffsetSec, kQuiescenceAmplitude, kTailSilenceThreshold, kDefaultCloudSeed, kEnvStageIdle/Attack/Hold/Release). Files snake_case: harmonic_cloud.h, harmonic_cloud_test.cpp, harmonic_cloud_spectral_test.cpp, harmonic_cloud_perf_test.cpp. k{Section}{Parameter}Id rule is N/A -- Phase 2 is Layer 3 DSP only, no VST3 parameters registered (plugin work starts Phase 8). |
| CC-warnings | pass | NOTE FIRST -- the project build proves less than it appears to: grep of dsp/tests/CMakeLists.txt for 'compile_options\|W4\|Wall\|Wextra\|WX' returns NOTHING, so dsp_systems_tests inherits no elevated warning level (only the KrateDSP lib target sets /W4 /permissive- at dsp/CMakeLists.txt:44-50, and harmonic_cloud.h is in NO KrateDSP TU -- grep -rln 'harmonic_cloud.h' --include=*.cpp returns only the 3 test files). I therefore compiled it independently at full warning levels rather than trust the target. (1) Project build, forced recompile: deleted build/windows-x64-release/dsp/tests/dsp_systems_tests.dir/Release/harmonic_cloud*.obj and rebuilt -- log shows the 3 TUs actually recompiling ('harmonic_cloud_perf_test.cpp', 'harmonic_cloud_spectral_test.cpp', 'harmonic_cloud_test.cpp') with ZERO warning/error lines; link then succeeded on retry ('dsp_systems_tests.vcxproj -> ...dsp_systems_tests.exe', EXIT=0; the one intervening LNK1104 was a transient file lock on the exe, not a code defect). (2) MSVC at the library's own strict level: cl /nologo /std:c++20 /W4 /permissive- /Zc:__cplusplus /EHsc /O2 /DNDEBUG /c /I dsp\include on a probe TU that constructs HarmonicCloud, calls prepare/setSeed/setRichness/setMutation/setDriftDepthCents/setStereoSpread/setEnvelopeOffsetSpread/setSpectralGravity/setInharmonicity/setSpectralTiltDb/noteOn/processStereoBlock/noteOff -> output was exactly one line, 'hc_warn_probe.cpp'. Zero warnings. Note the header's C4324 suppression is scoped push/pop (harmonic_cloud.h:34-37 and :1637-1639) and does not leak past the header. (3) GCC 13 via WSL, -Wall -Wextra -Wpedantic -Wno-unused-parameter -O2 -DNDEBUG -std=c++20 on the same probe: exit 0, no diagnostics. (4) All three real test TUs under g++ -Wall -Wextra (with the pffft/Catch2 include roots): zero diagnostics for harmonic_cloud_test.cpp, harmonic_cloud_spectral_test.cpp, harmonic_cloud_perf_test.cpp. (5) No regression from adding the header: full ./build/windows-x64-release/bin/Release/dsp_systems_tests.exe -> 'All tests passed (5999336 assertions in 1092 test cases)'. Repo lint gates all green: lint-float-bit-goldens 'clean (1370 files scanned)', lint-midi-timing-goldens 'clean (1370 files scanned)', lint-simd-aligned-loadstore 'clean -- 1370 file(s) scanned.', lint-arch-guarded-includes 'OK'. |
| CC-portability | pass | VERBATIM, default invocation as instructed -- `node tools/check-portability.js`, EXIT=0: 'check-portability: 23 translation unit(s) with g++' then OK lines for dsp/include/krate/dsp/processors/arpeggiator_core.cpp, dsp/tests/unit/processors/{arpeggiator_core_core_test, arpeggiator_core_curve_race_test, arpeggiator_core_ratcheting_test, arpeggiator_core_spicehuman_test, breathing_modulator_test, brownian_drift_test, growth_envelope_test, life_modulators_perf_test, midi_note_delay_test, orbit_modulator_test, spline_trajectory_test, tidal_modulator_test}.cpp, plugins/gradus/src/controller/controller_view_sync.cpp, plugins/gradus/src/processor/processor.cpp, plugins/gradus/tests/unit/processor/{audition_polyphony_test, boundary_metadata_test, event_cap_overflow_test, sequencer_transport_gating_test, set_active_flush_test, strum_offset_range_test, velocity_zero_noteon_test}.cpp, plugins/gradus/tests/unit/ui/ring_playhead_invalidation_test.cpp -- ending 'check-portability: all clear -- 23 compiled.' CRITICAL CAVEAT, DO NOT READ THAT AS PHASE-2 COVERAGE: not one of those 23 TUs is Phase-2 code. They are Phase-1 / Gradus leftovers. The tool discovers files via `git diff --name-only --diff-filter=ACMR origin/main...HEAD` (tools/check-portability.js:100) plus staged, and every Phase-2 file is UNTRACKED ('?? dsp/include/krate/dsp/systems/harmonic_cloud.h' etc. in git status), so `git diff` cannot see them -- the default run would have reported 'all clear' even if the new header did not compile under GCC at all. I therefore re-ran it explicitly. VERBATIM, explicit invocation `node tools/check-portability.js dsp/tests/unit/systems/harmonic_cloud_test.cpp dsp/tests/unit/systems/harmonic_cloud_spectral_test.cpp dsp/tests/unit/systems/harmonic_cloud_perf_test.cpp`, EXIT=0: 'check-portability: 3 translation unit(s) with g++' / 'OK      dsp/tests/unit/systems/harmonic_cloud_test.cpp' / 'OK      dsp/tests/unit/systems/harmonic_cloud_spectral_test.cpp' / 'OK      dsp/tests/unit/systems/harmonic_cloud_perf_test.cpp' / 'check-portability: all clear -- 3 compiled.' Supporting portability evidence: g++ 13 -std=c++20 -O2 with -Wall -Wextra -Wpedantic on a full-API probe -> exit 0 (the tool itself only passes -fsyntax-only -DNDEBUG -DRELEASE, see check-portability.js:148-149, i.e. it proves compilability, not warning-freedom -- covered under CC-warnings). -ffast-math trap avoided: no std::isnan/std::isinf/isfinite anywhere in the new code (grep hits are comments only at harmonic_cloud.h:781, perf_test.cpp:39,140,141, test.cpp:230,231,3259); the header uses detail::isNaN/isInf (db_utils.h:54,:174) and perf_test.cpp:142 defines a bit-pattern isFiniteValue(). No bit-exact float goldens (lint-float-bit-goldens: 'clean (1370 files scanned)'); no narrowing brace-init issues (GCC and clang-front-end MSVC /permissive- both accepted). SIMD alignment: harmonic_cloud.h:1544-1545 documents alignas(32) as locality-only because the kernel uses hn::LoadU/StoreU, and node tools/lint-simd-aligned-loadstore.js reports 'clean -- 1370 file(s) scanned.' REMAINING GAP I could not close locally: no arm64 verification. The dsp/CLAUDE.md recipe `clang++ --target=aarch64-unknown-linux-gnu -fsyntax-only` fails on this machine before reaching any project code -- 'dsp/include\krate/dsp/core/crossfade_utils.h:18:10: fatal error: cmath file not found' (no aarch64 sysroot installed). The arm64 leg of the macOS universal build is therefore unproven here; the mitigating evidence is that lint-arch-guarded-includes is clean (no krate include sits behind an arch #if, which is the failure class that leg uniquely catches) and the header includes nothing intrinsics-related. |

---

## Implementation notes — deviations reported by task agents

Grouped by theme. Every deviation below was reported by the task agent that made it; none is a threshold relaxation, and each is documented in-code at the site.

### Spec amendments (T001, plus the SC-005/SC-006 amendments landed later)

- **T001** applied the five plan §12 amendments to `spec.md` only (FR-031, FR-032, FR-035, FR-072, SC-014). Two mechanical deviations: FR-035's original trailing clause `without per-partial API calls in the audio path.` was dropped because the §12.1 replacement text ends with the same clause (a literal splice would duplicate it); and three edit seams were re-wrapped to the file's ~110-column width with no wording change.
- **T001 flagged as NOT touched** (outside its file list, for a later pass): the `### FR-030 series - per-partial BrownianDrift` heading (`spec.md:371`), the Assumptions passage at `:121-122` ("128 `BrownianDrift` instances per cloud"), and the requirement-trace table row near `:590` — all three still name the class rather than the lane behaviour.
- **SC-005 / SC-006** were amended in `spec.md` on 2026-07-26 after measurement, with the withdrawn wording quoted verbatim in a blockquote and each withdrawn clause pinned by a standing assertion phrased as its negation, so the original wording is restored if the finding ever inverts.

### Component design deviations

- **T002** added an MSVC `C4324` `#pragma warning(push/disable/pop)` around the class (precedent `harmonic_oscillator_bank.h:45-49`); the `alignas(32)` SoA arrays trigger it under `/W4`. Scoped push/pop, verified not to leak past the header.
- **T002** added private class-scoped `kEnvStageIdle/Attack/Hold/Release` (`static constexpr std::uint8_t`) — plan §1.3 pins the values 0..3 but names no symbols.
- **T002** chose `kDefaultCloudSeed = 1u` (§1.2 tabulates no value), matching `Xorshift32`'s own default constructor argument (`core/random.h:44`).
- **T002 reported a plan gap** (later closed by T008): plan §4.4 requires the raw `oa_i/od_i` envelope-offset draws stored per seed so onset *ordering* survives a spread change, but §1.3 named no array for them.
- **T002** noted `prepare()` did not call `updateDriftCoefficients` (the task's step list omitted it though plan §5 includes it); **T009** added both calls and reported it as load-bearing — `setDriftSmoothness()` early-returns on an unchanged value, so at the default 0.5 nothing else would ever configure the detune bank.
- **T004** changed the default `richness_` from the plan's 0.5f to **1.0f**. At r=0.5 only 8 partials are active, and the already-shipped SC-001/SC-002 cases `soloPartial(31)`/`soloPartial(63)` would render silence. Every Richness criterion sets the value explicitly.
- **T004** moved `reset()`'s tail-high-water retirement to *after* `recalculateAmplitudes()`.
- **T005 found and fixed a real component defect, not a threshold**: a cloud configured while silent rendered its first chunks with the FR-017 gain belonging to the *previous* `baseAmplitude_` set — measured onset peak exactly `2.0000` (`kOutputClamp`) against a 1.082 steady state, ~57 dB over-level at Richness 1 / tilt +12. Fix: `noteOn()` flushes the pending dirty recomputes and snaps `normGain_` **inside the existing quiescence branch only**, so a sounding retrigger still does not snap (SC-006's retrigger clause unaffected). Verified 2.0000 → 1.081.
- **T006** added an explicit `gravityIsZero ? n : std::pow(n, exponent)` identity branch — FR-081 requires the g=0 grid to be *exactly* integral, and under macOS `-ffast-math` `pow(n, 1.0f)` may be rewritten as `exp2(1.0f*log2(n))` and return 31.999998 for n=32. Same idiom as `tiltGain()`.
- **T008** moved `deriveSeed` from private to **public** `static constexpr` — neither the seed-distinctness case nor T009's `BrownianDrift` equivalence gate can be written otherwise, and re-deriving it in the test would defeat the point.
- **T008** added a non-negativity floor on both pan gains: plan §4.3's code as written **fails SC-012 clause 2**, because `kHalfPi` overshoots true π/2 by 4.37e-8 so `std::cos(1.0f*kHalfPi)` returns ≈ -4.37e-8 — a partial at position +1 came back with a vanishing phase-inverted left channel. `L²+R²` unchanged to ~1e-15.
- **T008** made `setSeed()` also clear `positionOverridden_` (FR-021/FR-008 scope an override to the next spread change, re-seed or `reset()`; `reset()` already cleared it, `setSeed` did not).
- **T009** replaced the blanket `recalculateAntiAliasing()` call in `updateControl` with a per-partial `updateAntiAliasGain(index)` path evaluated on the **detuned** frequency (plan deviation D5); `recalculateAntiAliasing()` itself remains for `reset()`.
- **T015** placed the FR-043 exact-zero guard on `currentAmplitude_` only, and **flagged a real property of the specced design**: the quiescent early-out short-circuits `updateControl` at `kQuiescenceAmplitude` (1e-5) while the guard fires at `kTailSilenceThreshold` (1e-8), so with small host blocks post-`noteOff` amplitudes park around 1e-6..1e-5 (non-denormal, and the kernel never processes them). Block-size-independent exact-zero after `noteOff` would require the early-out path to flush too — beyond plan §4.9 as written, so not added.

### Test-methodology deviations

- **T004** reported that the plan's SC-014 assertion **cannot hold on a correct implementation**: the MCF kernel's `sinState` is not unit-amplitude (`A²·cos²(w/2) = 1 + sin(w/2)·sin(2φ)`), so FR-016's random initial phase adds a per-partial gain spanning +1.60/-2.55 dB at partial 64. Verified numerically (worst per-partial law error 2.33 dB uncorrected vs 0.000 dB corrected). The test divides out `getPartialAntiAliasGain(i)·A_i`, recovering A from the orbit invariant through the shipped accessors — no part of the amplitude law is re-implemented. **T018** applied the same correction to SC-010 for the same reason (uncorrected, the 7040 Hz partial legitimately differs by up to ~1.7 dB between 44.1 and 96 kHz).
- **T006** added a mandatory **signal-presence guard** (`heterodyneMagnitude`, ≥50 % of rendered peak at the predicted frequency) before every SC-004 frequency assertion — without it a gravity-ignoring build would pass, because the estimator's stage-1 bracket is only ±5.86 Hz wide and its reference is the law's own prediction.
- **T007** ran SC-011 at **both** gravity signs (the plan says `|g| = 1`; n^1.1 and n^0.9 fold to different bins). It set drift depth 0 as a *precondition* of the bin mapping (`getPartialFrequencyHz` is the undetuned frequency; 50 cents at 20 kHz is ~880 bins), and flagged that SC-011 therefore does not gate plan risk R5.
- **T010, T011** pin **drift smoothness at 0** in the SC-015 and SC-016 measurement configs, and both reported this as load-bearing: at the shipped 0.5 default τ ≈ 15.1 s, a 60 s window holds ~4 correlation times and the sample Pearson r of two *independent* lanes scatters by ~0.5 — SC-015's `no pair |r| > 0.5` and SC-016's `|r| ≤ 0.3` would **reject a correct component**. At smoothness 0 the estimator scatter drops to ~0.062 / ~0.08. The spec pins no smoothness for either criterion.
- **T013** pins Mutation at 0 so "every partial is at full amplitude" is assertable as an exact bitwise identity rather than a tolerance; drift stays live at 25 cents so the renders are not static.
- **T013** widened the positive-control index window to `[inject-5, inject+6]` because `mergeAdjacentDetections` keeps the larger-amplitude member of a chain within `mergeGap` (`artifact_detection.h:240-249`).
- **T014** added an integer `std::bit_cast<uint32_t>(value) == poison.bits` assertion alongside each SC-017 poison feed, so a poison that failed construction fails loudly instead of making the pass a silent no-op. It also **flagged a macOS-only risk it could not remove within the task's constraints**: pass 2 depends on the component's inline `detail::isInf` rejecting ±Inf, and Apple clang under `-ffinite-math-only` can fold exactly that check to false; `tasks.md:50-52` and T020 explicitly forbid adding a `-fno-fast-math` source property for these TUs.
- **T015** engages solo *after* steady state and renders each phase as **one** `processStereoBlock` call — both load-bearing, per the early-out/guard threshold gap described above.
- **T016** uses one shared frozen control render for all six SC-006 clauses (justified by the detector's within-frame, scale-free statistic) and set the base Inharmonicity to 0.0 so the 220→440 Hz step measures FR-013's crossfade rather than FR-015's anti-alias ramp.
- **T017** reads the allocation liveness probe through a `volatile int*` before `delete[]` — C++14 N3664 lets MSVC/GCC elide an unobserved new/delete pair at -O2, which would red the liveness clause on a correctly wired detector. It also runs one warm-up block **outside** the tracked window for Highway's first-call ISA dispatch.
- **T019** shipped both perf baselines as explicitly-labelled **PROVISIONAL** plan projections (29000/31000) because it was instructed not to build; those were subsequently replaced by the measured 24000/26000 with the full provenance block now at `harmonic_cloud_perf_test.cpp:100-119`.
- **T019** confines the automation's richness lane to `[0.999, 1.0]` so the kernel really runs 64 partials throughout while both dirty flags still fire (the round-to-63 boundary is r = ln(63.5)/ln(64) = 0.99811).
- **T022** reported that `node tools/check-portability.js` is a **false green for Phase 2** in its default form: it discovers files via `git diff --name-only --diff-filter=ACMR origin/main...HEAD` (`tools/check-portability.js:100`), which only sees *tracked* files, and every Phase-2 file is currently untracked. The explicit-path invocation was run instead and is green. A pre-commit hook using `--staged` will cover them at commit time.

### Known residual risks flagged by task agents

- **T008**: SC-012's spread-0 clause bounds `max|L-R|` at 1e-7 and passes only if `std::cos` and `std::sin` return bit-identical values at `0.5f*kHalfPi` (sin lands 0.462 ULP away). A >0.46-ULP libm would split them; that would be a real FR-093 finding, not a tolerance to loosen.
- **T010**: SC-015 clause 2(a)'s 1e-5 cross-schedule tolerance could in principle straddle `OnePoleSmoother`'s `kCompletionThreshold = 1e-4` hard snap in the last ~70 ms of the 577-block render (estimated ~1 %). Prescribed response is to re-seed, never to relax the spec's tolerance.
- **T014**: the macOS `-ffinite-math-only` inline-guard-folding risk on SC-017 pass 2, described above.
- **CC-portability**: **no arm64 verification was possible locally** — the `dsp/CLAUDE.md` `clang++ --target=aarch64-unknown-linux-gnu -fsyntax-only` recipe fails before reaching project code (no aarch64 sysroot). The arm64 leg of the macOS universal build is unproven on this machine; mitigation is that `lint-arch-guarded-includes` is clean and the header includes nothing intrinsics-related.
- **SC-018**: the header comment at `harmonic_cloud_test.cpp:687-688` claiming a worst crest excess of +0.180 dB is **stale**; the live measurement is ~1.87 dB (still well inside the 6 dB bar).
- Untracked stray files unrelated to this phase were observed in `git status` by T002 and remain: a file literally named `console.log('` plus `20.0f)`, `95`, `i.verdict`, `kLobeHalfWidth)`, `slower`. Not created by any Phase-2 task; flagged for the human loop, not deleted.

---

## Remaining gates for the human loop

Phase 2 is Layer 3 DSP only — no plugin source changed, so **pluginval is not applicable** (it becomes a gate at Phase 8+ when the Seraphis plugin lands).

1. **clang-tidy** — not yet run for this phase. Regenerate the Ninja compile database if stale, then:
   ```
   cmake --preset windows-ninja
   ./tools/run-clang-tidy.ps1 -Target dsp -BuildDir build/windows-ninja
   ```
   Capture to a log on the first run; fix **all** warnings surfaced on the new header and the three new TUs before committing.
2. **Cheap CI gates** — already run green this session and worth re-running immediately before the commit, since the portability tool's file discovery changes behaviour once the files are staged:
   `node tools/check-portability.js` (will now see the Phase-2 TUs once staged), `lint-float-bit-goldens`, `lint-midi-timing-goldens`, `lint-simd-aligned-loadstore`, `lint-arch-guarded-includes`.
3. **Commit** — nothing has been committed. The tree currently carries:
   - modified: `dsp/tests/CMakeLists.txt` (T020's three-line source-list addition at `:332-334`)
   - untracked: `dsp/include/krate/dsp/systems/harmonic_cloud.h`, `dsp/tests/unit/systems/harmonic_cloud_test.cpp`, `harmonic_cloud_spectral_test.cpp`, `harmonic_cloud_perf_test.cpp`, `specs/seraphis-phase2-harmonic-cloud/`
   - also modified and **unrelated to this phase**: `.serena/project.yml` — do not sweep it into the phase commit.
   Branch check before committing: the current branch is `feat/seraphis-phase1-life-modulators`. Phase 2 should not land on a Phase 1 branch or on `main` — confirm or create the Phase 2 feature branch first.
