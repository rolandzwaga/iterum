# Seraphis Phase 10 — Compliance Report

**Phase:** 10 — Effects (spec slug `seraphis-phase10-effects`)
**Artifacts:** `specs/seraphis-phase10-effects/{spec.md, plan.md, tasks.md}`
**Date:** 2026-08-03
**Branch:** `feat/seraphis-phase1-life-modulators`

## Overall status: INCOMPLETE

One item is a hard **FAIL** (SC-015, both lenses). Three further residuals are recorded honestly
below because they affect the shippability of this phase even where no item is scored as failing.

### Gap list (read first)

1. **SC-015 — clang-tidy is not clean. FAIL.**
   `pwsh ./tools/run-clang-tidy.ps1 -Target seraphis -BuildDir build/windows-ninja` reports
   **Files analyzed: 25 / Errors: 0 / Warnings: 924**. SC-015 requires it clean.
   Zero warnings are in `plugins/seraphis/src` — all 924 are in `tests/`; 52 of them are in
   Phase 10's own new files (`effects_chain_test.cpp` 34, `state_v3_test.cpp` 16,
   `effects_perf_test.cpp` 2), the rest in files this phase modified. Because
   `run-clang-tidy.sh` has always listed the tests tree, the `.ps1` amendment made in this phase
   exposed a pre-existing breach rather than creating one — the criterion is still unmet.
   Dominant check: 851 × `modernize-use-designated-initializers`.

2. **SC-015 — the Linux/macOS lint and a full GCC/AppleClang warning-free build remain
   unverifiable on this host.** `bash tools/run-clang-tidy.sh --target seraphis` → EXIT=1,
   `[ERROR] clang-tidy not found`. `check-portability.js`'s g++ compile of 15 TUs is partial
   evidence only. The `.ps1` and `.sh` file sets are now identical, so the 924 is the best
   available proxy for the `.sh`.

3. **Undeclared scope: `dsp/` was modified.** `git status --porcelain dsp/` (re-run for this
   report) returns `M dsp/include/krate/dsp/effects/spectral_delay.h` and
   `M dsp/include/krate/dsp/primitives/stft.h`. The spec's Non-goals state "No `dsp/` behaviour
   change… Phase 10 admits none", and FR-017's own text cites that prohibition. SC-019 passes
   partly *because of* the `spectral_delay.h` one-hop delay compensation. `SpectralDelay` is also
   consumed by `plugins/iterum/src/processor/processor.h` and
   `plugins/ruinae/src/engine/ruinae_effects_chain.h`, so the change reaches two shipping
   plugins. `dsp_effects_tests` (1505 cases) and `dsp_primitives_tests` (477 cases) both pass
   after the edit — but the change is undeclared scope and needs a phase-owner ruling before
   commit.

4. **Release bookkeeping is half-landed.** `plugins/seraphis/CHANGELOG.md` gained a
   `## [0.3.0] - 2026-08-03` section (FR-038), but `plugins/seraphis/version.json` still reads
   `"version": "0.2.0"` (verified for this report). T030 flagged this as an open item for the
   release step; it was outside that task's file list.

Everything else — 49 FR rows, 20 SC rows under the FR lens, 20 of 21 SC rows under the SC lens,
and all 5 cross-cutting constraint rows — is **pass** with the verbatim evidence recorded below.

---

## Compliance table

Lens key: **fr** = requirement-implementation verification; **sc** = success-criterion execution
(measured/observed); **cc** = cross-cutting constraint.

| ID | Lens | Verdict | Evidence |
| --- | --- | --- | --- |
| FR-001 | fr | PASS | plugins/seraphis/src/processor/processor.cpp:1852-1943 — renderSlice runs macros→engine_->processStereoBlock (:1862) → reverb_->processStereoBlock (:1865) → per-output-sample master gain (:1876-1880) → step 4c send+wander inside one scoped timer (:1894-1927) → engine_->processOutputStage (:1931) → bloom (:1955-1966). SC-002's exact-equality negative control passes (seraphis_tests.exe: All tests passed, 379320 assertions in 46 test cases). |
| FR-002 | fr | PASS | processor.cpp:1931 processOutputStage is the last audio-touching call; the only post-output stage call is processor.cpp:1936-1938 guarded by effectsStageAfterOutput_, declared test-only at processor.h:1023 and set only by detail::SeraphisEffectsStageBypassProbe (processor.h:1015-1024, friend at :457). |
| FR-003 | fr | PASS | processor.cpp:2068 spectralDelay_.process(fxChunkL_.data(), fxChunkR_.data(), …) runs in place on the prepare-time scratch pair (processor.h:680), and the return is summed into the bus at :2105-2110 `busL[i] += fxOutL_[r] * g`. The bus pointers are never passed to process(). |
| FR-003a | fr | PASS | processor.cpp:2068 passes the literal constant kFxSendChunkSamples (processor.h:145, = SpectralDelay hop 512); the sole grid is fxChunkFill_ (:2054-2062, no second counter — grep for a free-running fxPhase_ returns nothing); clearFifos() (:2420-2440) pre-fills the output FIFO with one chunk of silence and is the only clear point. SC-017 "The effects send is block-size invariant" (effects_chain_test.cpp:2186) passes with its negative control at :2223. |
| FR-004 | fr | PASS | processor.cpp:614 `spectralDelay_.setDryWetMix(1.0f);` precedes :616 `spectralDelay_.prepare(sampleRate_, bound);`. No kFx*Id maps to setDryWetMix in handleEffectsParamChange (effects_params.h:161-248). effects_chain_test.cpp:1380 "the send carries no current-block dry" passes. |
| FR-005 | fr | PASS | processor.cpp:1341 `return static_cast<uint32>(reverb_->getLatencySamples());` — unchanged. SC-004 test processor_bus_test.cpp:128-190 drives all 16 IDs non-default and sweeps mix 0→1→0→1→0 asserting the report equals reverb_->getLatencySamples() every block; passes. |
| FR-006 | fr | PASS | processor.cpp:2247-2248 `globalMs_.process(busL + off, busR + off, busL + off, busR + off, chunkLen)` (in-place), followed by the per-sample azimuth gain pair at :2253-2254. |
| FR-007 | fr | PASS | processor.cpp:1902-1907: `if (fxSendRuns_) { runSendStage(...) }` — fxSendRuns_ is `fxSendState_ != Bypassed` (:2409). Nothing reads or writes fxIn*/fxOut*/fxChunk* outside runSendStage. SC-018 clause (e) (param_cadence_test.cpp:1599 and :1616) asserts sendChunkCountForTest() stays 0 at defaults and stops advancing after the drain; both pass. |
| FR-008 | fr | PASS | processor.cpp:2027-2040 — the reset fires only when `fxResetDue_ && fxChunkFill_ + n >= kFxSendChunkSamples` (a fill-chunk boundary), incrementing spectralDelayResets_; fxResetDue_ is set at :2361 as `std::cmp_greater(fxBypassedSamples_, fxSendDrainSamples_) && !freezeOn`. clearFifos() is the only FIFO clear. SC-018(a) (param_cadence_test.cpp:1348) passes. |
| FR-009 | fr | PASS | The return gain is the class-(b) OnePoleSmoother fxReturnGainSm_ configured at kFxReturnRampMs (processor.h:151, `inline constexpr float kFxReturnRampMs = kParamSmoothMs;` with static_assert at :152), read (never re-advanced) at processor.cpp:2098-2100. SC-008's send-bypass transition is inside the six measured (effects_chain_test.cpp:4632-4641) and passes. |
| FR-009a | fr | PASS | processor.cpp:2049-2054 pushes zeros while `fxSendState_ != Active`; the drain window is processor.cpp:2398-2408 (`fxDrainRemaining_ <= 0 \| \| fxDrainPeak_ < kFxSendDrainFloor`), constants named at processor.h:156 (kFxSendDrainMs = SpectralDelay::kMaxDelayMs) and :160 (kFxSendDrainFloor = 1.0e-6f). effects_chain_test.cpp:1469 "the drain's energy floor ends the window early" and SC-011a (:3942) pass. |
| FR-010 | fr | PASS | processor.cpp:2161-2165 returns before globalMs_.process and the azimuth multiply when !fxWanderRunsEffective_; the raw predicate is at :1125-1127 (`width != kDefaultWidth \| \| wanderDepth != 0.0f \| \| azimuthDepth != 0.0f`). SC-002's `maxAbsDiff == 0.0f` (effects_chain_test.cpp:2153-2154) passes, which is only satisfiable if the stage truly does not run. |
| FR-010a | fr | PASS | processor.cpp:2185-2190 — on !fxWanderWasActive_ the stage pushes kDefaultWidth, calls globalMs_.reset() and snaps both azimuth smoothers to 1.0 before the first processed sample; the disengage latch is wanderAtIdentity() (:2280-2303). Both transitions are in SC-008's six (effects_chain_test.cpp:4632-4641); effects_chain_test.cpp:2005 "the wander disengage does not step the image" passes. |
| FR-011 | fr | PASS | processor.cpp:1161-1166 advances both drifts by the whole block when the wander stage will not run, and :2214-2216 advances them one 64-sample chunk per grid boundary when it does — mutually exclusive, so no double advance. SC-018(b) (param_cadence_test.cpp:1422) asserts widthDriftBlockCountForTest() equals the process() block count under both bypass states; passes. |
| FR-012 | fr | PASS | processor.cpp:2323 `++bypassPredicateEvals_;` is the first statement of updateEffectsBypassState(), which is called once per process() call at processor.cpp:1069-1071 (outside the slice loop that starts at :1307). SC-018(c) (param_cadence_test.cpp:1461) asserts the counter equals the call count, not the slice count; passes. |
| FR-013 | fr | PASS | plugin_ids.h:184-199 declares exactly the 16 C-6 enumerators at 1400/1410-1419/1430/1440-1443; :280 `kEffectsParamRangeEnd = 1500`; ladder static_assert extended at :290 and band gate added at :314-316. |
| FR-014 | fr | PASS | plugins/seraphis/src/parameters/effects_params.h — struct EffectsParams of 12 atomic<float>/2 atomic<int>/2 atomic<bool> at :125-141, then handleEffectsParamChange (:161), registerEffectsParams (:254), formatEffectsParam (:331), saveEffectsParams (:414), loadEffectsParams (:440), loadEffectsParamsToController (:492) — the same six-function shape as aether_params.h. |
| FR-015 | fr | PASS | effects_params.h:52-59 aliases every range constant to the DSP symbol with the source line cited (`kFxDelayTimeMaxMs = Krate::DSP::SpectralDelay::kMaxDelayMs; ///< == :92 (2000)`), :104-119 does the same for defaults (kFxSaturationDefault = SeraphisEngine::kOutputSaturation, kFxWanderRateDefault = BrownianDrift::kDefaultSmoothness). No re-typed literals at the registration sites (:254-319). |
| FR-016 | fr | PASS | effects_params.h:71 `inline constexpr float kFxDelayFeedbackMax = 0.95f;` — the sole denominator of ID 1414's registration (:280-282) and of its denormalization (:200-201). SpectralDelay::kMaxFeedback (1.2f) is never registered. |
| FR-016a | fr | PASS | effects_params.h:90 `tiltCompensatedFeedback(float fb, float tilt)` is the named helper, gated at :95-97 by `static_assert(tiltCompensatedFeedback(0.95f, ±1.0f) == 0.475f && tiltCompensatedFeedback(0.95f, 0.0f) == 0.95f)`. The push is processor.cpp:1761 `spectralDelay_.setFeedback(Seraphis::tiltCompensatedFeedback(feedback, tilt))`, recomputed when either ID moves. effects_chain_test.cpp:2423 "the feedback is pushed tilt-compensated" and SC-005 clause 1 (:3227, 513-bin sweep) pass. |
| FR-017 | fr | PASS | dropdown_mappings.h:245-246 kFxSpreadDirectionLabels (3 entries, U+2192 as → UCNs), :265-267 kFxDelaySyncNoteLabels = {1/64T,1/64,1/64D,1/32T,1/32,1/32D,1/16T,1/16,1/16D,1/8T}; kSpreadDirectionCount at :228 with the enum-tie static_assert at :230-233; kFxDelaySyncNoteDefaultIndex = 7 at :276; the compile-time gate `static_assert(Krate::DSP::dropdownToDelayMs(kFxDelaySyncNoteDefaultIndex, 120.0) == 125.0f)` at :288. kSyncNoteLabels is not reused. Build succeeds, so both static_asserts hold. |
| FR-018 | fr | PASS | processor.cpp:1547-1552 — one more `else if (id < kEffectsParamRangeEnd)` rung on the existing ladder, calling handleEffectsParamChange + markDirty; no switch over 107 IDs. |
| FR-019 | fr | PASS | processor.cpp:134 `enum class Route : std::uint8_t { VP, MB, AE, CFG, ENG, FX, Local };`, routeOf returns Route::FX at :275 for 1410-1443, and markDirty's switch arm at :2479 bumps no generation counter (banner at :2471-2478). SC-018(d) (param_cadence_test.cpp:1529) asserts applyVoiceParamsCallCountForTest()/applyAetherParamsCallCountForTest() stay unmoved; passes. |
| FR-020 | fr | PASS | plugin_ids.h:217 the legend header now reads "All 107 registered IDs, grouped by type (85 R + 14 L + 8 T = 107)", with the new rows at :234-237 (R), :252-253 (L: 1413, 1419) and :266-267 (T: 1418, 1430). No pre-existing ID's type row changed (git diff shows only additions to the legend). |
| FR-021 | fr | PASS | pushEffectsParams() is the sole writer: processor.cpp:1666-1675 pushes `soft ? effectsParams_.saturation : 0.0f` on change, incrementing the retained Phase 9 counter engSoftLimitPushes_ at :1674. The pushGlobalParams() block is removed (processor.cpp:1597 "NO setOutputSaturation BLOCK HERE"). The prepare-time push at :570-573 uses the same composed value and seeds lastPushedSaturation_ with lastPushedSaturationValid_ = false (:579-580). state_v3_test.cpp:679 "D-2: kSoftLimitId gates the amount ID 1400 supplies, and never reverts it" passes. |
| FR-022 | fr | PASS | processor.cpp:1720-1791 pushes setBaseDelayMs/setSpreadMs/setDiffusion/setStereoWidth/setFeedback/setFeedbackTilt/setTimeMode/setNoteValue/setSpreadDirection, each behind its own change tracker. effects_chain_test.cpp:2322 "every SpectralDelay setter receives its registered value" and :2378 "an unchanged value is not re-pushed" pass. |
| FR-023 | fr | PASS | processor.cpp:1819 `spectralDelay_.setFreezeEnabled(freezeReady);` — driven only from effectsParams_.spectralFreeze. effects_chain_test.cpp:2506 "the Aether freeze does not engage the spectral freeze" and :2536 the converse both pass. |
| FR-023a | fr | PASS | processor.cpp:2334-2336: `wantActive = (mix != 0.0f) \| \| freezeOn;` and `fxEffectiveReturnGain_ = freezeOn ? std::max(mix, kFxFreezeMinReturnGain) : mix;` (constant at processor.h:165 = 0.5f); the forced engage suppresses the reset via `&& !freezeOn` at :2361. SC-007 arm (a) "engaging 1430 alone on the shipped patch holds audible output" (effects_chain_test.cpp:3697) passes. |
| FR-024 | fr | PASS | processor.cpp:2197-2245 — the control loop walks the absolute 64-sample grid (kWanderControlChunkSamples) and calls equalPowerGains once per chunk (:2239); the products are targets for azimuthGainLSm_/RSm_ configured at kParamSmoothMs (processor.cpp:714-715) and applied per sample at :2253-2254. effects_chain_test.cpp:1777 "the azimuth pan pair is evaluated on the 64-sample grid" passes. |
| FR-024a | fr | PASS | All five named inline constexpr with cited justifications: kWanderWidthSpanPercent (processor.h, used at :2226), kParamSmoothMs for the azimuth pair (processor.cpp:714-715), kFxWidthDriftSalt/kFxAzimuthDriftSalt (processor.h:222-223 with a distinctness static_assert at :224), kFxAzimuthCentreComp = 1.41421356f (processor.h:193, applied to BOTH gains at processor.cpp:2243-2244). Depth is a plugin-side multiply (:2226, :2234) — setDepth() is never called (effects_chain_test.cpp:1866 "depth never reaches BrownianDrift::setDepth" passes); setMean(0.0f) is pushed to both drifts (:693 and the width-drift sibling). effects_chain_test.cpp:1716 "azimuth is unity at centre" passes. |
| FR-025 | fr | PASS | processor.cpp:1831-1832 pushes wanderRate to widthDrift_.setSmoothness and azimuthDrift_.setSmoothness in the same block. effects_chain_test.cpp:2470 "the wander rate reaches both drifts" passes. |
| FR-026 | fr | PASS | processor.cpp:695 `azimuthDrift_.setSeed(kSeedValues[fxSeedIndex] ^ kFxAzimuthDriftSalt)` (width sibling immediately above) at prepare, and :1705 on the kSeedId change path inside pushEffectsParams()/the seed burst. Salts are distinct by static_assert (processor.h:224). |
| FR-027 | fr | PASS | seedRng THEN reset, at all three sites: processor.cpp:634-635 (after prepare at :616), :1695-1696 (kSeedId change on the audio thread), :857-858 (setActive(false)). SC-010 "Effects renders are seed-deterministic" (effects_chain_test.cpp:4432) uses two independently heap-allocated fixtures and passes, including the >1e-3 seed-discrimination clause. |
| FR-028 | fr | PASS | setupProcessing() prepares every Phase 10 member: spectralDelay_.prepare (processor.cpp:616), globalMs_.prepare (:705), azimuthDrift_.prepare (:691) and its width sibling, the azimuth smoothers (:714-717), and the FIFO/scratch/tap vectors sized to the constants (processor.h:678-680, kFxFifoCapacity = 4096 with a capacity static_assert at :232). |
| FR-029 | fr | PASS | runSendStage/runWanderStage index through .data()/operator[] only (processor.cpp:2044-2113, 2247-2262); no .at(), no allocation, no lock. clang-tidy -Target seraphis: "Errors: 0, Warnings: 0" over entry/processor/controller. SC-011 "Effects stage is RT-safe" (effects_perf_test.cpp:711) passes its zero-allocation clause. |
| FR-030 | fr | PASS | processor.cpp:1054-1066 builds fxBlockCtx_ once per process() call with the three-part guard `pc != nullptr && (pc->state & kTempoValid) != 0 && pc->tempo > 0.0`, falling back to 120.0, using designated initializers. It is passed at :2069. SC-019's fallback clause (effects_chain_test.cpp:3354 "an invalid transport falls back to 120 BPM") passes. |
| FR-031 | fr | PASS | plugin_ids.h:26-28 — kStateVersion1 = 1, kStateVersion2 = 2 (new), kCurrentStateVersion = 3; :315 `static_assert(kStateVersion1 < kStateVersion2 && kStateVersion2 < kCurrentStateVersion, ...)`. |
| FR-032 | fr | PASS | effects_params.h:414-437 saveEffectsParams appends 12 floats + 4 int32 (64 bytes) after every v2 field; loadEffectsParams (:440) and loadEffectsParamsToController (:492) mirror it; controller.cpp:105 calls the controller loader last. SC-009(d) (state_v3_test.cpp:465-479) asserts v3 minus v2 == kEffectsBlockBytes and byte-identical from offset 4; passes. Minor deviation from C-8's prose: the block groups the 12 floats then the 4 int32 rather than strict C-6 interleave — documented at effects_params.h:404-412 and self-consistent across all three functions. |
| FR-033 | fr | PASS | state_v3_test.cpp:435 "(b) A version-2 blob loads, effects at C-6 defaults, Phase 9 fields intact" (REQUIRE(migrated.setState(v2.get()) == kResultOk) at :450) and :498 "(c) A 36-byte version-1 blob still loads"; both pass. Field-by-field default checks at :287-303. |
| FR-034 | fr | PASS | processor.cpp:2888 invalidates every effects push tracker inside pushAllSurfaces(). state_v3_test.cpp:521 "FR-034: a preset loaded after prepare re-pushes the composed saturation" and :574 "…re-pushes the effects surface to the DSP" both pass. |
| FR-035 | fr | PASS | processor.cpp:857-947 (setActive(false)/reset path): seedRng+reset on spectralDelay_ (:857-858), the setter/snapParameters pair (:885-896), azimuthDrift_.reset() (:928) and its width sibling, globalMs_.setWidth(kDefaultWidth)+reset() (:944-945), and azimuthGainLSm_/RSm_.snapTo(1.0f) (:946-947). |
| FR-036 | fr | PASS | plugins/seraphis/resources/editor.uidesc:122-138 — 16 <control-tag> entries under `<!-- Effects (1400+) -->`, inside the existing <control-tags> block; the diff adds no <view> and no layout element. |
| FR-037 | fr | PASS | controller.cpp:54 `registerEffectsParams(parameters);` and :144 `if (formatEffectsParam(id, valueNormalized, string) == kResultOk) return kResultOk;`; the controller loader is wired at :105. parameter_surface_test.cpp:503 `CHECK(controller.getParameterCount() == 107)` passes. |
| FR-038 | fr | PASS | plugins/seraphis/CLAUDE.md band table row now reads `\| 1400+ \| Effects \| 10 — shipped \|`; plugins/seraphis/CHANGELOG.md gains a `## [0.3.0] - 2026-08-03` section (91 added lines) describing the send, sync labels, freeze and wander. |
| FR-038a | fr | PASS | All nine discharged: (1) editor_lifecycle_test.cpp:242 and :254 == 107; (2) parameter_surface_test.cpp:503 == 107 (plus static_assert kSurfaceRowCount == 107 at :232); (3-4) param_perf_test.cpp:1097 `static_assert(kNonDefaultTable.size() == 107)` with countRows re-derived at :1099-1110 and the operational most-expensive-end dataset proven live by `Seraphis_EffectsTable_IsTheWorstCase` (:2601, passes) under the PHASE 10 BASELINE PROVENANCE banner at :745-828; (5) param_continuity_test.cpp:564 `static_assert(std::size(kContinuityMechanism) == 101, "SC-005: 107 registered…")`; (6) :590 `== 12`; (7) :802 and :904 `REQUIRE(count == 107)`; (8) the both-ways CHECK(table == expected) passes with all 16 rows; (9) the per-ID sweep at :895-912 walks all 107 registered IDs and no effects ID is in kExemptIds (:574-579, still exactly six). |
| FR-038b | fr | PASS | param_continuity_test.cpp:494-558 — 13 class-(a) rows whose Evidence matches the spec table exactly (1413 Structural, 1430 Ramp, 1442 CoefficientOnly, the rest Smoother) each with a file:line citation, and 3 class-(b) rows (1410, 1441, 1443) at kBodySmoothMs, which is `Seraphis::kParamSmoothMs` (:174 = 20.0f, processor.h:131). kClassBIds (:581-589) lists exactly those three additions. ID 1410's ramp is one smoother by construction (processor.h:151-152 static_assert kFxReturnRampMs == kParamSmoothMs). |
| FR-039 | fr | PASS | plugins/seraphis/tests/CMakeLists.txt:29-36 adds unit/state_v3_test.cpp, integration/effects_chain_test.cpp, integration/effects_perf_test.cpp to the existing seraphis_tests target (no new executable); :98-106 puts effects_chain_test.cpp and state_v3_test.cpp on -fno-fast-math and deliberately keeps effects_perf_test.cpp off it. |
| FR-040 | fr | PASS | processor.h:276 `struct SeraphisEffectsStageBypassProbe;` / :457 `friend struct detail::SeraphisEffectsStageBypassProbe;`, with exactly the three capabilities as members at :1022-1024 (effectsStageBypassed_, effectsStageAfterOutput_, effectsReturnRampSnap_), consumed at processor.cpp:1902 / :1912+1936 / :2095-2097. All three default false; the probe is defined only in the test TU (banner at processor.h:251-275 records the grep). |
| FR-041 | fr | PASS | processor.h:390 effectsStageNsForTest, :398 effectsStageProcessCallsForTest (incremented once per process() call — banner at processor.cpp:1177-1183), :402 spectralDelayResetCountForTest, :406 effectsPushCountForTest, :408 widthDriftBlockCountForTest, :412 bypassPredicateEvalCountForTest, :420 sendChunkCountForTest, :431/:434 preOutputTapLForTest/preOutputTapRForTest (clause 6, split per channel), plus :444 preOutputTapTruncatedForTest. The tap copy is at processor.cpp:1915-1921 immediately before processOutputStage. effects_chain_test.cpp:1263 "the pre-output tap is honest about truncation" exercises a >2048 block and passes. |
| SC-001 | fr | PASS | param_denorm_test.cpp:429 "Seraphis effects parameters denormalize" — SECTION :486 asserts the 107-row surface and every C-6 row, :535 the normalized→plain→normalized round-trip, :606 "The effects label tables are the shipped strings, in order". Full run: seraphis_tests.exe → "All tests passed (379320 assertions in 46 test cases)". |
| SC-001a | fr | PASS | param_continuity_test.cpp compiles with static_assert(std::size(kContinuityMechanism) == 101) at :564 and static_assert(std::size(kClassBIds) == 12) at :590; REQUIRE(count == 107) passes at :802 and :904; kExemptIds is still the six Phase 9 IDs (:574-579). The whole file is green in the 46-case run. |
| SC-002 | fr | PASS | effects_chain_test.cpp:2088 "Effects defaults are a no-op on the same build" — one instance, warm-up + reprepare on both arms, non-vacuity guard REQUIRE(livePeak > 0.0f) at :2151, then `CHECK(maxAbsDiff(bypassedL, fx.capturedL) == 0.0f)` and the R channel at :2153-2154. Passes. |
| SC-003 | fr | PASS | effects_chain_test.cpp:1553 clause (a) — precondition REQUIRE(shipped.worstTapPeak > kLimiterCeilingLin) at :1568 then CHECK(worstOutputPeak <= allowedPeak) on the TRUE OUTPUT, with the mandatory positive control (step 5 after step 6) asserted to FAIL at :1586; :1603 clause (b) 6.0206 dB ± kOrderGainToleranceDb = 0.1 (:861-862) at the tap; :1660 clause (c) side-energy ± kOrderSideToleranceDb = 0.5 (:876). All pass. |
| SC-004 | fr | PASS | processor_bus_test.cpp:128 "Phase 10 does not change reported latency" — three SECTIONs (all 16 IDs non-default, mix sweep 0→1→0→1→0, re-prepare) each REQUIRE getLatencySamples() == reverb_->getLatencySamples() every block. The zero-restartComponent clause is discharged structurally at :94-100 (an AudioEffect holds no IComponentHandler; there is no call site). Passes. |
| SC-005 | fr | PASS | effects_chain_test.cpp:3216 — clause 1 sweeps all 513 bins over tilt {−1,−0.5,0,+0.5,+1} at fb 0.95 (:3227, kDecayBinCount static_assert at :2661); clause 2 runs tilt −1 (:3269) and tilt +1 (:3273) as separate 120 s runs at kDecay60dB = 1.0e-3 with the delay pinned to kDefaultDelayMs by static_assert (:2685-2688); clause 3 uses kDecayShapeRiseRatio = 1.0592537 (+0.5 dB) with a −180 dBFS floor. Non-vacuity floors at :2709 and :2713. All pass. |
| SC-006 | fr | PASS | effects_chain_test.cpp:4166 "Effects at maxima respect the true-peak ceiling" — all 16 IDs driven to normalized 1.0 (static_assert kEffectsParamIdCount == 16 at :4108, plus per-row maximum static_asserts at :4122-4130), asserted against kLimiterCeilingLin = 0.8912509f with kCeilingAllowanceDb = 0.1f (:845). Passes. |
| SC-007 | fr | PASS | effects_chain_test.cpp:3691 — arm (a) ":3697 engaging 1430 alone on the shipped patch holds audible output" (> −60 dBFS via kMinusSixtyDbfs = 1.0e-3, :3484, within kOneDbRatio = 1.1220185, :3485) and arm (b) ":3753 a freeze engaged over a live send holds level and centroid" (± 1 dB plus kCentroidTolerance = 0.05, :3491, and ≥ 30 dB decay with freeze off via kMinusThirtyDbRatio, :3487). Measured at the tap through kFreezePipelineBlocks (:3428-3432). Both pass. |
| SC-008 | fr | PASS | effects_chain_test.cpp:4888 — six transitions (kClickTransitionCount = 6, :4640), windows centred at event + 1024 + 512 + 1024 = 2560 (static_assert :4569), six same-length quiescent references, bound = kClickBoundFactor(1.5) × max(reference) (:4933), bit-pattern non-finite clause (:4909), plus BOTH mandatory positive controls — detector wiring with a 2× injected step (:4937-4960) and criterion wiring via the probe's ramp-snap capability. Non-vacuity REQUIREs at :4922-4923. Passes. |
| SC-009 | fr | PASS | state_v3_test.cpp:379 — SECTION (a) :406 exact 16-field round-trip (memcmp of two v3 blobs), (b) :435 v2 blob loads with effects at C-6 defaults and Phase 9 fields intact, (c) :498 36-byte v1 blob still loads, (d) :465 v3[4..] has v2[4..] as a strict prefix with `CHECK(v3->getSize() - v2->getSize() == kEffectsBlockBytes)` and `CHECK(memcmp(v3, v2, 4) != 0)`. All pass. |
| SC-010 | fr | PASS | effects_chain_test.cpp:4432 "Effects renders are seed-deterministic" — two independently heap-allocated fixtures (banner :4433-4436), preconditions REQUIRE(first.sendChunks > 0) and wanderRan (:4444-4447), agreement at render_fingerprint.h tolerances, and the seed-discrimination clause at kDetDiscriminationBound = 100 × kMetricTolerance (:4347). Passes. |
| SC-011 | fr | PASS | I executed the seven-run protocol myself on an idle host (no competing processes; `tasklist \| grep -i seraphis/cl.exe/MSBuild/cmake` returned nothing before the runs). Seven consecutive `build/windows-x64-release/bin/Release/seraphis_tests.exe "[.perf]"`, ALL EXIT=0, all "All tests passed (504 assertions in 7 test cases)". HOST EQUIVALENCE, not assumed: the SC-008 arm-1 calibrator read 82.95 / 66.80 / 73.25 / 66.45 / 68.95 / 66.95 / 65.85 ns, worst 82.95, against the 2026-08-02 fresh-boot cold reference worst of 82.40 ns (param_perf_test.cpp:160) — a 1.007x ratio, i.e. this host IS the protocol's machine (the earlier 4.15e6-ns failures were contention from two resident seraphis_tests.exe instances, now gone). MY worst-of-seven for every SC-011 row, from the printed banners: worst FR-008 reset block 115500/113500/115000/82100/103600/117300/109800 -> 117300 ns = 1.100 % of one core <= kBurstBudgetNs 533333; worst seedRng()+reset() block 8900/9200/9300/9100/9100/9500/9200 -> 9500 ns = 0.089 % <= 533333; worst OTHER block 66800/65000/65900/64700/65900/67000/67700 -> 67700 ns = 0.635 % <= kStageBudgetNs 266667. Medians recorded per run as the criterion requires (FR-008 median 75200-96400 ns; seed median 8100-8400 ns). BOTH PREVIOUSLY-MISSING CLAUSES ARE NOW INSTRUMENTED. (1) EXCEPTIONS are counted, not reasoned about: effects_perf_test.cpp:872-879 wraps `fx.processBlock(kBlock)` in try/catch(...) incrementing `t.exceptions`, asserted `REQUIRE(t.exceptions == 0u)` (:942 region, 88th line of the case body). (2) LOCKS are scanned: `scanRtPathSources()` (effects_perf_test.cpp:698-755) reads the 6 audio-thread-reachable files (processor.cpp, processor.h, effects_params.h, spectral_delay.h, stft.h, brownian_drift.h) via SERAPHIS_SRC_DIR/KRATE_DSP_INCLUDE_DIR (tests/CMakeLists.txt:86-87), strips comments, and counts 11 lock tokens (std::mutex, recursive_mutex, shared_mutex, lock_guard, unique_lock, scoped_lock, shared_lock, condition_variable, pthread_mutex, ".lock()", "->lock()") and 5 throw tokens; asserted lockPrimitives==0, throwSites==0, filesScanned==6, filesMissing==0, codeBytes>100000, witnesses>=2 — plus a positive/negative control that proves the scanner detects (2 std::mutex, 1 lock_guard, 1 throw) and that the comment-stripper suppresses them. Allocations: REQUIRE(t.allocations == 0u) over kRtBlocks=5625 blocks x 512 = 2 880 000 samples = exactly 60 s; toggles >=100 on send/wander/freeze, seedWrites >=16, fr008Count >=16, seedCount >=16, processCalls==predicateEvals==5625. NOTE (not a gap): the render is 2 voices, and SC-011's text specifies no voice count — the 8-voice requirement lives in SC-012/SC-013, both of which assert activeVoices==kPolyphony. |
| SC-012 | fr | PASS | Worst-of-seven executed by me under the SC-013 protocol (same seven idle runs as SC-011; calibrator worst 82.95 ns vs cold reference 82.40, param_perf_test.cpp:160). "Effects cost nothing at defaults" PASSED in all seven. Per-run measured ns/block from the printed banners: 73 / 159 / 115 / 91 / 77 / 74 / 90 -> WORST 159 ns = 0.00149 % of one core, against kDefaultsBudgetNs = 10666.67 ns (0.10 %) — 1.5 % of budget consumed. Gate: effects_perf_test.cpp:1204 REQUIRE(arm.bestNsPerBlock <= kDefaultsBudgetNs). Structural assertions all present and passing: :1195 REQUIRE(fx->proc->effectsStageProcessCallsForTest() == arm.harnessCalls) (FR-041 clause 1 divisor identity — per process() CALL, never slices); :1197 REQUIRE(arm.sendChunks == 0u) (FR-007's exact prohibition at C-6 defaults); :1156 REQUIRE(arm.activeVoices == kPolyphony) — so the defaults arm runs at the pinned 8-voice operating point, not zero voices. THE PREVIOUSLY-MISSING DELIVERABLE NOW EXISTS: the BASELINE PROVENANCE table is FILLED (effects_perf_test.cpp:121-130, headed "** FILLED 2026-08-03, SEVEN-RUN IDLE DATASET, ALL SEVEN RUNS EXIT=0 **"); its transcribed SC-012 row reads 94/69/79/73/76/82/112 worst 112, and my independent seven-run dataset (worst 159) reproduces it within run-to-run noise. The banner states its machine honestly — idle but not rebooted, with "the words 'fresh-boot' are not claimed here" — and justifies equivalence with the same calibrator instrument I re-measured. |
| SC-013 | fr | PASS | Worst-of-seven executed by me; "Effects stage stays inside its 2.5 % budget" PASSED in all seven idle runs. Per-run measured ns/block: 46796 / 48161 / 46876 / 47299 / 49819 / 46329 / 47897 -> WORST 49819 ns = 0.467 % of one core against kStageBudgetNs = 266666.67 ns (2.5 %) — 18.7 % of budget consumed, 216 848 ns of headroom. Gate: effects_perf_test.cpp:1338 REQUIRE(arm.bestNsPerBlock <= kStageBudgetNs). Operating point verified, not assumed: :1287 REQUIRE(arm.activeVoices == kPolyphony) (8 voices sounding, Phase 9's SC-009 MIDI script), :1290 REQUIRE(fx->proc->sendChunkCountForTest() > 0u) (the stage is genuinely running, all 16 rows at maxima), :1332 the divisor identity effectsStageProcessCallsForTest() == arm.harnessCalls — load-bearing here because this render carries automation on 1410/1441/1443 and therefore subdivides into 64-sample slices. THE CRITERION'S OWN MANDATED DELIVERABLE IS NOW DELIVERED — this was the sole prior gap. effects_perf_test.cpp:60-61 no longer reads "** NOT YET FILLED **"; it reads "BASELINE PROVENANCE - SC-013's per-run table ** FILLED 2026-08-03, SEVEN-RUN IDLE DATASET, ALL SEVEN RUNS EXIT=0 **", and :122-130 carry the full nine-row-by-seven-column table with a worst and ceiling column (transcribed SC-013 row: 48994/49790/48267/46624/47635/48723/47883, worst 49790 — my independent worst of 49819 agrees to 0.06 %). The banner also documents rather than hides the superseded hot dataset (:93-104), including the SC-011 OTHER-block breach it contained, and records that the ceiling was never touched — "the machine was". Budget derivation intact at :157-165 (20.91 % + 2.5 = 23.41 %, 1.59 points inside the 25 % ceiling). |
| SC-014 | fr | PASS | REVERSED FROM FAIL — the prior 4.15e6 ns / 38.9 % readings were host contention, not a regression. `Seraphis_FullPoly_CpuBudget_WithFullSurface` (gate at param_perf_test.cpp:2621, REQUIRE(measuredNs <= kFullPolyCeilingNs)) PASSED in all seven of my consecutive idle runs. Per-run measured: 2.33683e6 (21.9078 %) / 2.33620e6 (21.9019 %) / 2.29427e6 (21.5088 %) / 2.38098e6 (22.3217 %) / 2.24666e6 (21.0625 %) / 2.22751e6 (20.8829 %) / 2.31127e6 (21.6682 %) -> WORST-OF-SEVEN 2 380 980 ns = 22.3217 % of one core against kFullPolyCeilingNs = 2 666 666.7 ns (25 %). That is 2.68 POINTS of margin, and it composes exactly as SC-013's budget predicted: Phase 9's pinned cold worst was 2 230 830 ns = 20.91 % with 91 rows, so Phase 10 composes in at +1.41 points against the 2.5 points reserved. HOST IS THE PROTOCOL'S MACHINE, PROVEN NOT ASSERTED: SC-008 arm 1 calibrator worst 82.95 ns across my seven runs vs the 2026-08-02 fresh-boot cold reference worst 82.40 ns (param_perf_test.cpp:160) = 1.007x. THE GATE WAS NOT RELAXED TO ACHIEVE THIS — `git diff plugins/seraphis/tests/integration/param_perf_test.cpp \| grep -E '^[-+].*(kFullPolyCeilingNs\|kBaselineFullPolyNs\|kRegressionFactor)\s*='` returns ONLY an added comment line; the live constants are unchanged: kFullPolyCeilingNs = kBlockBudgetNs * 0.25 (:392), kRegressionFactor = 1.15 (:395), kBaselineFullPolyNs = 2318840.0 (:472). The run-time regression gate (2 318 840 x 1.15 = 2 666 666 ns) also passed. Table grown as specified: static_assert(kNonDefaultTable.size() == 107) at param_perf_test.cpp:1097, and the companion prover `Seraphis_EffectsTable_IsTheWorstCase` PASSED in all seven runs — its per-candidate scores are printed (e.g. "candidate 0 : score 1.01893 (median 49700 ns per 512-sample send chunk)" vs "candidate 2000 : score 0.981074"), so the costlier end is measured rather than asserted. Figures also transcribed into effects_perf_test.cpp:135-136 (2307790..2366810, worst 2366810 = 22.19 %), which my independent worst of 2 380 980 reproduces to 0.6 %. |
| SC-015 | fr | **FAIL** | TWO CLAUSES PASS, ONE IS NOW DEMONSTRABLY BREACHED (it was merely unverified before). PASS — `node tools/check-portability.js` -> EXIT=0, "check-portability: all clear -- 12 compiled"; and because the three new files are untracked and skipped by the changed-files scan, I re-ran them explicitly -> EXIT=0, "all clear -- 3 compiled". PASS — warning-free MSVC build, and this time proven by FORCED recompilation, not an incremental no-op: my first build recompiled 0 TUs, so I touched all 22 changed/new sources plus the two DSP headers and rebuilt; /f/tmp/rv_build2.log shows 24 seraphis TUs recompiled (controller.cpp, processor.cpp, entry.cpp, effects_chain_test.cpp, effects_perf_test.cpp, state_v3_test.cpp, param_perf_test.cpp, ... ) with `grep -ci warning` = 0, and /f/tmp/rv_dspbuild.log shows all five DSP layer suites (28 TUs) rebuilt with `grep -ci warning` = 0. FAIL — clang-tidy is NOT clean. `pwsh ./tools/run-clang-tidy.ps1 -Target seraphis -BuildDir build/windows-ninja` now analyzes 25 files (the script was amended this phase to add plugins/seraphis/tests so it matches run-clang-tidy.sh:151-152) and reports "Files analyzed: 25 / Errors: 0 / Warnings: 924". SC-015 requires it "clean". Breakdown: 0 warnings in plugins/seraphis/src (grep count = 0) — every one of the 924 is in tests/; by check, 851 modernize-use-designated-initializers, 12 modernize-use-auto, 7 readability-use-std-min-max, 6 readability-use-anyofallof, 5 performance-enum-size, 5 modernize-return-braced-init-list, 5 cppcoreguidelines-missing-std-forward, 4 modernize-loop-convert, 4 cppcoreguidelines-avoid-non-const-global-variables, 3 bugprone-implicit-widening-of-multiplication-result, others. NOT PURELY INHERITED: 52 of the 924 are in Phase 10's OWN new files (effects_chain_test.cpp 34, state_v3_test.cpp 16, effects_perf_test.cpp 2); the rest sit in files this phase modified (parameter_surface_test.cpp 224, param_reach_test.cpp 154, param_perf_test.cpp 120, param_continuity_test.cpp 109, state_v2_test.cpp 97, param_cadence_test.cpp 56, ...). Because run-clang-tidy.sh has ALWAYS listed the tests tree, these warnings were already reaching the Linux/macOS pre-commit lint — the .ps1 amendment exposed a pre-existing breach rather than creating one, but the criterion is still unmet. STILL UNVERIFIABLE ON THIS HOST: `bash tools/run-clang-tidy.sh --target seraphis` -> EXIT=1, "[ERROR] clang-tidy not found"; and a warning-free GCC/AppleClang FULL build cannot be observed on Windows (check-portability's g++ compile of 15 TUs is partial evidence only). The two scripts do now analyze an identical file set, so the .ps1's 924 is the best available proxy for the .sh. |
| SC-016 | fr | PASS | BOTH CLAUSES NOW VERIFIED — the 10-cycle shortfall is fixed. CLAUSE 1: `tools/pluginval.exe --strictness-level 5 --validate build/windows-x64-release/VST3/Release/Seraphis.vst3` (run against the bundle rebuilt from current sources in this session — /f/tmp/rv_build2.log:63 "Seraphis.vcxproj -> ...\\VST3\\Release\\Seraphis.vst3\\Contents\\x86_64-win\\Seraphis.vst3") -> EXIT=0, `grep -c "Completed tests in"` = 19 (all 19 groups completed), `grep -ci fail` = 0. CLAUSE 2: the harness now runs the criterion's own number. plugins/seraphis/tests/unit/controller/editor_lifecycle_test.cpp:253 passes /*cycles=*/10 (previously 3), with the comment at :244-251 explaining why the count is load-bearing (a VST3Editor parameter attach/detach refcount leak drifts per cycle, so a 3-cycle run sees a third of the excursion). Surface count asserted intact on both sides of the cycles: :242 and :259 REQUIRE(controller.getParameterCount() == 107). Executed: `seraphis_tests.exe "[lifecycle]"` -> EXIT=0, "All tests passed (66 assertions in 2 test cases)". The companion case at :126 remains at 3 cycles, which is a different (non-SC-016) arm. |
| SC-017 | fr | PASS | effects_chain_test.cpp:2186 "The effects send is block-size invariant" — SECTION :2254 compares block sizes 1, 2, 3, 7, 512, 2048 and one 4096 call against a contiguous reference at render_fingerprint.h's kSampleTolerance (1.0e-4f), and SECTION :2223 is the mandatory negative control (accumulator bypassed → asserted to FAIL). Both pass. |
| SC-018 | fr | PASS | param_cadence_test.cpp:1343 "Effects push cadence" — all five clauses present and green: (a) :1348, (b) :1422, (c) :1461, (d) :1529, (e) :1599 (defaults → sendChunkCountForTest() stays 0) and :1616 (advances only while active or draining). |
| SC-019 | fr | PASS (with scope caveat) | effects_chain_test.cpp:3281 "Synced delay tracks host tempo" — :3290 estimator self-test on a known comb, :3324 the label table names the periods measured, :3339 a synced send at 90/140 BPM including the default index 7, :3354 the invalid-transport 120 BPM fallback. All pass. CAVEAT, and it is a scope violation rather than a criterion failure: passing at ±512 samples was achieved partly by editing dsp/ — `git diff dsp/include/krate/dsp/effects/spectral_delay.h` changes `delayFrames` to `max(0, … − 1.0f)` (one-hop delay compensation) and restructures the analyse/synthesise loop, and `dsp/include/krate/dsp/primitives/stft.h` gains a capacity guard in pullSamples(). The spec's Non-goals state "No dsp/ behaviour change… Phase 10 admits none", and FR-017's own text cites that prohibition. SpectralDelay is also consumed by plugins/iterum/src/processor/processor.h and plugins/ruinae/src/engine/ruinae_effects_chain.h, so the delay-time change reaches two shipping plugins. dsp_effects_tests (1505 cases) and dsp_primitives_tests (477 cases) both pass after the edit, but the change is undeclared scope. |
| SC-001 | sc | PASS | seraphis_tests.exe "Seraphis effects parameters denormalize" (plugins/seraphis/tests/unit/param_denorm_test.cpp:486,535,567,606) PASSES. Actual: REQUIRE(controller.getParameterCount() == 107) -> "107 == 107"; round-trip over {0,0.25,0.5,0.75,1} e.g. "0.1111111111 == Approx(0.1111111111)" under margin(1.0e-6); REQUIRE(kFxSpreadDirectionLabels.size() == 3u) -> "3 == 3" and REQUIRE(kFxDelaySyncNoteLabels.size() == 10u) -> "10 == 10" with every element CHECKed literally against param_denorm_test.cpp:402-407 = {u"Low → High",u"High → Low",u"Center → Out"} and {"1/64T","1/64","1/64D","1/32T","1/32","1/32D","1/16T","1/16","1/16D","1/8T"} — exactly the spec strings; REQUIRE(kFxDelaySyncNoteDefaultIndex == 7) -> "7 == 7" and label[7] == u"1/16". ID 1400 info: id 1400==1400, stepCount 0==0, default 0.150000006 == Approx(0.15), kCanAutomate set. |
| SC-001a | sc | PASS | seraphis_tests.exe "Seraphis_ContinuityMechanism_CoversEveryInScopeId,Seraphis_ParameterAutomation_IsClickFree,Seraphis_ParameterAutomation_IsClickFree_PositiveControls" -> "All tests passed (2058 assertions in 4 test cases)". Tables read directly: param_continuity_test.cpp:564 static_assert(std::size(kContinuityMechanism) == 101); :590 static_assert(std::size(kClassBIds) == 12) with :582-589 listing exactly the three new IDs kFxDelayMixId, kFxWanderDepthId, kFxAzimuthDepthId added to Phase 9's nine; :574-577 kExemptIds still exactly six (kSeedId + 408-412), NO effects ID; :802 and :904 REQUIRE(count == 107). Implementation side widened: processor.h:613 std::array<Krate::DSP::OnePoleSmoother*, 12> classBSmoothers(). Both-ways gate CHECK(table == expected) and the per-ID automation sweep passed inside those 2058 assertions. |
| SC-002 | sc | PASS | seraphis_tests.exe "Effects defaults are a no-op on the same build" (effects_chain_test.cpp:2088) PASSES. Actual, both channels over 480256 samples (10.005 s @ 48 kHz): :2153 CHECK(maxAbsDiff(bypassedL, fx.capturedL) == 0.0f) -> "0.0f == 0.0f"; :2154 same for R -> "0.0f == 0.0f". Non-vacuous: :2151 REQUIRE(livePeak > 0.0f) -> "0.28845f > 0.0f". Both arms are the same Processor instance with the FR-040 probe engaged/disengaged, as the criterion requires. |
| SC-003 | sc | PASS | seraphis_tests.exe "Effects chain order matches C-1" PASSES, all three clauses with real numbers. (a) limiter-last, measured on TRUE output (effects_chain_test.cpp:1553-1587): precondition REQUIRE(shipped.worstTapPeak > kLimiterCeilingLin) -> "2.03794f > 0.89125f" (limiter provably in gain reduction); assertion CHECK(shipped.worstOutputPeak <= allowedPeak) -> "0.89125f <= 0.90157f"; mandatory positive control (step 5 after step 6) CHECK(control.worstOutputPeak > allowedPeak) -> "1.63217f > 0.90157f", i.e. it fails as required. (b) at preOutputTapForTest(), REQUIRE_FALSE(preOutputTapTruncatedForTest()) -> "!false": isolated-return RMS 0.0191796621 -> 0.0383593242 on a master-gain doubling, CHECK(\|deltaDb - 6.02\| < 0.1) -> "0.0000000867 < 0.1"; linear-region guard 0.10587f/0.21173f <= 0.63096f. (c) isolated-return side energy 0%=0, 100%=1.53558, 200%=6.14232 (monotone, ratio exactly 4.0), CHECK(\|wideDeltaDb - ideal\| < 0.5) -> "0.0000008366 < 0.5", and mono-side bound "0.0 < 0.0000000154". |
| SC-004 | sc | PASS | seraphis_tests.exe "Phase 10 does not change reported latency" (processor_bus_test.cpp:128) PASSES, 3 sections. Actual: baseline == reverb->getLatencySamples() -> "1024 (0x400) == 1024 (0x400)", held identical after driveAllEffectsIdsNonDefault() (all 16 IDs, :107-124) across 8 further blocks, across the mix sweep {1.0,0.0,1.0,0.0} (:176-182), and across a re-prepare at 44100 Hz (:185-199) — every check "1024 == 1024". The zero-restartComponent clause is discharged by absence of a call site: grep -rn "restartComponent" plugins/seraphis/src/ returns only the explanatory comment at processor.h:640, no invocation (a Vst::AudioEffect holds no IComponentHandler). |
| SC-005 | sc | PASS | seraphis_tests.exe "Spectral delay decays at registered max feedback" PASSES, all three parts. (1) Derived bound over tilt in {-1,-0.5,0,+0.5,+1}: CHECK(worst < 1.0f) -> "0.95f < 1.0f" five times, and the uncompensated control reproduces the spec's Overview fact 6 exactly: CHECK(binsAboveUnity == 243) -> "243 == 243". (2) Decay at 250 ms / fb 0.95 / diffusion 1.0, two runs, isolated return at preOutputTapForTest(): tilt -1 "peak=0.0617623 final=1.24767e-21 (required <= 6.17623e-05)"; tilt +1 "peak=0.0617078 final=2.07721e-21 (required <= 6.17078e-05)" — i.e. ~-274 dB against a -60 dB requirement. (3) Shape CHECK(current <= previous * kDecayShapeRiseRatio + kDecayShapeFloor) passed on every 5 s window. Non-finite checked by bit pattern. |
| SC-006 | sc | PASS | seraphis_tests.exe "Effects at maxima respect the true-peak ceiling" PASSES. Actual: CHECK(worstOutputPeak <= ceiling) -> "0.82322f <= 0.90157f" (kLimiterCeilingLin 0.8912509 + kCeilingAllowanceDb 0.1); non-vacuous CHECK(worstOutputPeak >= kMaximaAudibleFloor) -> "0.82322f >= 0.1f"; CHECK(nonFinite == 0) passed; the effects section provably ran, CHECK(sendChunkCountForTest() > 0) -> "2820 (0xb04) > 0" = 2820 x 512 = 1443840 samples = 30.08 s, matching the criterion's 30 s render. |
| SC-007 | sc | PASS | seraphis_tests.exe "Spectral freeze holds the Aether tail" PASSES, both arms at preOutputTapForTest(). Arm (a) from the C-6 defaults (send bypassed, REQUIRE(bus.sendChunks == 0)): isolated-return RMS 5 s after note-off = 0.0179310667, CHECK(> kMinusSixtyDbfs) -> "0.0179310667 > 0.001", and inside the +/-1.0 dB band "0.0179310667 >= 0.0161619158" / "0.0179310667 <= 0.0203466481". Arm (b) live send: "frozen RMS: A=0.0324716 B=0.0321556" = -0.085 dB drift, band [0.0289402994, 0.0364336813]; "centroid: A=331.315 Hz B=332.718 Hz", CHECK(\|dC\| <= 5% * C) -> "1.4037070543 <= 16.5657304929" (0.42 %); freeze-off control "unfrozen RMS: A=0.0201125 B=2.93273e-05" -> CHECK(offB <= offA * kMinusThirtyDbRatio) -> "0.0000293273 <= 0.000636013" (-56.7 dB). Ordering asserted: CHECK(ordering.frozenAtEngage), CHECK(ordering.chunksBeforeEngage == 0). |
| SC-008 | sc | PASS | seraphis_tests.exe "Effects transitions are click-free" PASSES. Six transitions enumerated at effects_chain_test.cpp:4632-4641 (send-engage, freeze-on, freeze-off, wander-bypass engage, wander-bypass disengage, send-bypass) with the output-domain window shift and 6 reference draws each side: "SC-008: worst test window is \"wander-bypass engage\" at 0.0150173 against 6 draws each side"; CHECK(stats.maxTest <= bound) -> "0.01502f <= 0.01769f" where bound = 1.5 x maxRef 0.01179f. Both mandatory positive controls fire: detector wiring CHECK(detected > bound) -> "0.0349f > 0.01769f"; criterion wiring with the ramp snapped to instant, CHECK_FALSE(snappedStats.maxTest <= snappedBound) passes, i.e. the render fails clause 3 as required. Latency precondition REQUIRE(getLatencySamples() == kClickReverbLatency) -> "1024 == 1024". |
| SC-009 | sc | PASS | seraphis_tests.exe "Seraphis_StateVersion3_RoundTripsAndMigrates" (plugins/seraphis/tests/unit/state_v3_test.cpp) PASSES. All four clauses present as sections: :406 "(a) All 16 effects fields survive getState -> setState exactly" (12 float + 4 int fields each CHECKed); :435 "(b) A version-2 blob loads, effects at C-6 defaults, Phase 9 fields intact" — got.f[0..11] == kFx*Default and got.i[0..3] == the four discrete defaults; :498 "(c) A 36-byte version-1 blob still loads"; :465 "(d) The v2 blob is a strict prefix of the v3 blob from offset 4 on". Non-vacuity gate at :391 "Every one of the 16 driven values differs from its C-6 default" — 16 CHECK(reference != defaults). Blob sizes asserted (kEffectsBlockBytes, kV3StateBytes). |
| SC-010 | sc | PASS | seraphis_tests.exe "Effects renders are seed-deterministic" PASSES. Two independently heap-allocated Processor instances, 20 s (sendChunks 1880 == 1880, 1880 x 512 = 962560 samples = 20.05 s), freeze and wander exercised (REQUIRE(first.wanderRan), REQUIRE(second.sendChunks == first.sendChunks)): "SC-010 agreement: L metric=0 sample=0 (); R metric=0 sample=0 ()", CHECK(agreeL.withinTolerance()) -> true, CHECK(agreeR.withinTolerance()) -> true against render_fingerprint.h's kSampleTolerance 1.0e-4 / kMetricTolerance 1.0e-5. Seed discrimination (the negative control for FR-027): "SC-010 (i): seed index 3 vs 9 worst relative aggregate-metric difference" CHECK(worst > kDetDiscriminationBound) -> "0.1689492184 > 0.001" and salt-swap "0.1070640171 > 0.001", both >> 100 x kMetricTolerance. No bit-exact golden used. |
| SC-011 | sc | PASS | I executed the seven-run protocol myself on an idle host (no competing processes; `tasklist \| grep -i seraphis/cl.exe/MSBuild/cmake` returned nothing before the runs). Seven consecutive `build/windows-x64-release/bin/Release/seraphis_tests.exe "[.perf]"`, ALL EXIT=0, all "All tests passed (504 assertions in 7 test cases)". HOST EQUIVALENCE, not assumed: the SC-008 arm-1 calibrator read 82.95 / 66.80 / 73.25 / 66.45 / 68.95 / 66.95 / 65.85 ns, worst 82.95, against the 2026-08-02 fresh-boot cold reference worst of 82.40 ns (param_perf_test.cpp:160) — a 1.007x ratio, i.e. this host IS the protocol's machine (the earlier 4.15e6-ns failures were contention from two resident seraphis_tests.exe instances, now gone). MY worst-of-seven for every SC-011 row, from the printed banners: worst FR-008 reset block 115500/113500/115000/82100/103600/117300/109800 -> 117300 ns = 1.100 % of one core <= kBurstBudgetNs 533333; worst seedRng()+reset() block 8900/9200/9300/9100/9100/9500/9200 -> 9500 ns = 0.089 % <= 533333; worst OTHER block 66800/65000/65900/64700/65900/67000/67700 -> 67700 ns = 0.635 % <= kStageBudgetNs 266667. Medians recorded per run as the criterion requires (FR-008 median 75200-96400 ns; seed median 8100-8400 ns). BOTH PREVIOUSLY-MISSING CLAUSES ARE NOW INSTRUMENTED. (1) EXCEPTIONS are counted, not reasoned about: effects_perf_test.cpp:872-879 wraps `fx.processBlock(kBlock)` in try/catch(...) incrementing `t.exceptions`, asserted `REQUIRE(t.exceptions == 0u)` (:942 region, 88th line of the case body). (2) LOCKS are scanned: `scanRtPathSources()` (effects_perf_test.cpp:698-755) reads the 6 audio-thread-reachable files (processor.cpp, processor.h, effects_params.h, spectral_delay.h, stft.h, brownian_drift.h) via SERAPHIS_SRC_DIR/KRATE_DSP_INCLUDE_DIR (tests/CMakeLists.txt:86-87), strips comments, and counts 11 lock tokens (std::mutex, recursive_mutex, shared_mutex, lock_guard, unique_lock, scoped_lock, shared_lock, condition_variable, pthread_mutex, ".lock()", "->lock()") and 5 throw tokens; asserted lockPrimitives==0, throwSites==0, filesScanned==6, filesMissing==0, codeBytes>100000, witnesses>=2 — plus a positive/negative control that proves the scanner detects (2 std::mutex, 1 lock_guard, 1 throw) and that the comment-stripper suppresses them. Allocations: REQUIRE(t.allocations == 0u) over kRtBlocks=5625 blocks x 512 = 2 880 000 samples = exactly 60 s; toggles >=100 on send/wander/freeze, seedWrites >=16, fr008Count >=16, seedCount >=16, processCalls==predicateEvals==5625. NOTE (not a gap): the render is 2 voices, and SC-011's text specifies no voice count — the 8-voice requirement lives in SC-012/SC-013, both of which assert activeVoices==kPolyphony. |
| SC-011a | sc | PASS | seraphis_tests.exe "A short mix excursion preserves the send tail" PASSES, isolated return at preOutputTapForTest(). 200 ms excursion at mix 1.0 / fb 0.6 / 250 ms with a captured freeze: recovered RMS 0.0560468015 inside the +/-2.0 dB band, CHECK(a.recover >= a.pre / kTwoDbRatio) -> "0.0560468015 >= 0.0443577302" and CHECK(a.recover <= a.pre * kTwoDbRatio) -> "0.0560468015 <= 0.0703022633"; centroid CHECK(\|after-before\| <= 5% * before) -> "2.5112826065 <= 16.8884420893" (0.74 %); CHECK(a.resets == 0) and CHECK(a.nonFinite == 0). The discriminating control (excursion longer than kFxSendDrainMs) fires: CHECK(l.immediate <= l.pre * kMinusThirtyDbRatio) -> "0.000000056 <= 0.0016768204", CHECK(l.resets == 1) -> "1 == 1", and it rebuilds (CHECK(l.recover > l.pre * kMinusEightDbRatio)). |
| SC-012 | sc | PASS | Worst-of-seven executed by me under the SC-013 protocol (same seven idle runs as SC-011; calibrator worst 82.95 ns vs cold reference 82.40, param_perf_test.cpp:160). "Effects cost nothing at defaults" PASSED in all seven. Per-run measured ns/block from the printed banners: 73 / 159 / 115 / 91 / 77 / 74 / 90 -> WORST 159 ns = 0.00149 % of one core, against kDefaultsBudgetNs = 10666.67 ns (0.10 %) — 1.5 % of budget consumed. Gate: effects_perf_test.cpp:1204 REQUIRE(arm.bestNsPerBlock <= kDefaultsBudgetNs). Structural assertions all present and passing: :1195 REQUIRE(fx->proc->effectsStageProcessCallsForTest() == arm.harnessCalls) (FR-041 clause 1 divisor identity — per process() CALL, never slices); :1197 REQUIRE(arm.sendChunks == 0u) (FR-007's exact prohibition at C-6 defaults); :1156 REQUIRE(arm.activeVoices == kPolyphony) — so the defaults arm runs at the pinned 8-voice operating point, not zero voices. THE PREVIOUSLY-MISSING DELIVERABLE NOW EXISTS: the BASELINE PROVENANCE table is FILLED (effects_perf_test.cpp:121-130, headed "** FILLED 2026-08-03, SEVEN-RUN IDLE DATASET, ALL SEVEN RUNS EXIT=0 **"); its transcribed SC-012 row reads 94/69/79/73/76/82/112 worst 112, and my independent seven-run dataset (worst 159) reproduces it within run-to-run noise. The banner states its machine honestly — idle but not rebooted, with "the words 'fresh-boot' are not claimed here" — and justifies equivalence with the same calibrator instrument I re-measured. |
| SC-013 | sc | PASS | Worst-of-seven executed by me; "Effects stage stays inside its 2.5 % budget" PASSED in all seven idle runs. Per-run measured ns/block: 46796 / 48161 / 46876 / 47299 / 49819 / 46329 / 47897 -> WORST 49819 ns = 0.467 % of one core against kStageBudgetNs = 266666.67 ns (2.5 %) — 18.7 % of budget consumed, 216 848 ns of headroom. Gate: effects_perf_test.cpp:1338 REQUIRE(arm.bestNsPerBlock <= kStageBudgetNs). Operating point verified, not assumed: :1287 REQUIRE(arm.activeVoices == kPolyphony) (8 voices sounding, Phase 9's SC-009 MIDI script), :1290 REQUIRE(fx->proc->sendChunkCountForTest() > 0u) (the stage is genuinely running, all 16 rows at maxima), :1332 the divisor identity effectsStageProcessCallsForTest() == arm.harnessCalls — load-bearing here because this render carries automation on 1410/1441/1443 and therefore subdivides into 64-sample slices. THE CRITERION'S OWN MANDATED DELIVERABLE IS NOW DELIVERED — this was the sole prior gap. effects_perf_test.cpp:60-61 no longer reads "** NOT YET FILLED **"; it reads "BASELINE PROVENANCE - SC-013's per-run table ** FILLED 2026-08-03, SEVEN-RUN IDLE DATASET, ALL SEVEN RUNS EXIT=0 **", and :122-130 carry the full nine-row-by-seven-column table with a worst and ceiling column (transcribed SC-013 row: 48994/49790/48267/46624/47635/48723/47883, worst 49790 — my independent worst of 49819 agrees to 0.06 %). The banner also documents rather than hides the superseded hot dataset (:93-104), including the SC-011 OTHER-block breach it contained, and records that the ceiling was never touched — "the machine was". Budget derivation intact at :157-165 (20.91 % + 2.5 = 23.41 %, 1.59 points inside the 25 % ceiling). |
| SC-014 | sc | PASS | REVERSED FROM FAIL — the prior 4.15e6 ns / 38.9 % readings were host contention, not a regression. `Seraphis_FullPoly_CpuBudget_WithFullSurface` (gate at param_perf_test.cpp:2621, REQUIRE(measuredNs <= kFullPolyCeilingNs)) PASSED in all seven of my consecutive idle runs. Per-run measured: 2.33683e6 (21.9078 %) / 2.33620e6 (21.9019 %) / 2.29427e6 (21.5088 %) / 2.38098e6 (22.3217 %) / 2.24666e6 (21.0625 %) / 2.22751e6 (20.8829 %) / 2.31127e6 (21.6682 %) -> WORST-OF-SEVEN 2 380 980 ns = 22.3217 % of one core against kFullPolyCeilingNs = 2 666 666.7 ns (25 %). That is 2.68 POINTS of margin, and it composes exactly as SC-013's budget predicted: Phase 9's pinned cold worst was 2 230 830 ns = 20.91 % with 91 rows, so Phase 10 composes in at +1.41 points against the 2.5 points reserved. HOST IS THE PROTOCOL'S MACHINE, PROVEN NOT ASSERTED: SC-008 arm 1 calibrator worst 82.95 ns across my seven runs vs the 2026-08-02 fresh-boot cold reference worst 82.40 ns (param_perf_test.cpp:160) = 1.007x. THE GATE WAS NOT RELAXED TO ACHIEVE THIS — `git diff plugins/seraphis/tests/integration/param_perf_test.cpp \| grep -E '^[-+].*(kFullPolyCeilingNs\|kBaselineFullPolyNs\|kRegressionFactor)\s*='` returns ONLY an added comment line; the live constants are unchanged: kFullPolyCeilingNs = kBlockBudgetNs * 0.25 (:392), kRegressionFactor = 1.15 (:395), kBaselineFullPolyNs = 2318840.0 (:472). The run-time regression gate (2 318 840 x 1.15 = 2 666 666 ns) also passed. Table grown as specified: static_assert(kNonDefaultTable.size() == 107) at param_perf_test.cpp:1097, and the companion prover `Seraphis_EffectsTable_IsTheWorstCase` PASSED in all seven runs — its per-candidate scores are printed (e.g. "candidate 0 : score 1.01893 (median 49700 ns per 512-sample send chunk)" vs "candidate 2000 : score 0.981074"), so the costlier end is measured rather than asserted. Figures also transcribed into effects_perf_test.cpp:135-136 (2307790..2366810, worst 2366810 = 22.19 %), which my independent worst of 2 380 980 reproduces to 0.6 %. |
| SC-015 | sc | **FAIL** | TWO CLAUSES PASS, ONE IS NOW DEMONSTRABLY BREACHED (it was merely unverified before). PASS — `node tools/check-portability.js` -> EXIT=0, "check-portability: all clear -- 12 compiled"; and because the three new files are untracked and skipped by the changed-files scan, I re-ran them explicitly -> EXIT=0, "all clear -- 3 compiled". PASS — warning-free MSVC build, and this time proven by FORCED recompilation, not an incremental no-op: my first build recompiled 0 TUs, so I touched all 22 changed/new sources plus the two DSP headers and rebuilt; /f/tmp/rv_build2.log shows 24 seraphis TUs recompiled (controller.cpp, processor.cpp, entry.cpp, effects_chain_test.cpp, effects_perf_test.cpp, state_v3_test.cpp, param_perf_test.cpp, ... ) with `grep -ci warning` = 0, and /f/tmp/rv_dspbuild.log shows all five DSP layer suites (28 TUs) rebuilt with `grep -ci warning` = 0. FAIL — clang-tidy is NOT clean. `pwsh ./tools/run-clang-tidy.ps1 -Target seraphis -BuildDir build/windows-ninja` now analyzes 25 files (the script was amended this phase to add plugins/seraphis/tests so it matches run-clang-tidy.sh:151-152) and reports "Files analyzed: 25 / Errors: 0 / Warnings: 924". SC-015 requires it "clean". Breakdown: 0 warnings in plugins/seraphis/src (grep count = 0) — every one of the 924 is in tests/; by check, 851 modernize-use-designated-initializers, 12 modernize-use-auto, 7 readability-use-std-min-max, 6 readability-use-anyofallof, 5 performance-enum-size, 5 modernize-return-braced-init-list, 5 cppcoreguidelines-missing-std-forward, 4 modernize-loop-convert, 4 cppcoreguidelines-avoid-non-const-global-variables, 3 bugprone-implicit-widening-of-multiplication-result, others. NOT PURELY INHERITED: 52 of the 924 are in Phase 10's OWN new files (effects_chain_test.cpp 34, state_v3_test.cpp 16, effects_perf_test.cpp 2); the rest sit in files this phase modified (parameter_surface_test.cpp 224, param_reach_test.cpp 154, param_perf_test.cpp 120, param_continuity_test.cpp 109, state_v2_test.cpp 97, param_cadence_test.cpp 56, ...). Because run-clang-tidy.sh has ALWAYS listed the tests tree, these warnings were already reaching the Linux/macOS pre-commit lint — the .ps1 amendment exposed a pre-existing breach rather than creating one, but the criterion is still unmet. STILL UNVERIFIABLE ON THIS HOST: `bash tools/run-clang-tidy.sh --target seraphis` -> EXIT=1, "[ERROR] clang-tidy not found"; and a warning-free GCC/AppleClang FULL build cannot be observed on Windows (check-portability's g++ compile of 15 TUs is partial evidence only). The two scripts do now analyze an identical file set, so the .ps1's 924 is the best available proxy for the .sh. |
| SC-016 | sc | PASS | BOTH CLAUSES NOW VERIFIED — the 10-cycle shortfall is fixed. CLAUSE 1: `tools/pluginval.exe --strictness-level 5 --validate build/windows-x64-release/VST3/Release/Seraphis.vst3` (run against the bundle rebuilt from current sources in this session — /f/tmp/rv_build2.log:63 "Seraphis.vcxproj -> ...\\VST3\\Release\\Seraphis.vst3\\Contents\\x86_64-win\\Seraphis.vst3") -> EXIT=0, `grep -c "Completed tests in"` = 19 (all 19 groups completed), `grep -ci fail` = 0. CLAUSE 2: the harness now runs the criterion's own number. plugins/seraphis/tests/unit/controller/editor_lifecycle_test.cpp:253 passes /*cycles=*/10 (previously 3), with the comment at :244-251 explaining why the count is load-bearing (a VST3Editor parameter attach/detach refcount leak drifts per cycle, so a 3-cycle run sees a third of the excursion). Surface count asserted intact on both sides of the cycles: :242 and :259 REQUIRE(controller.getParameterCount() == 107). Executed: `seraphis_tests.exe "[lifecycle]"` -> EXIT=0, "All tests passed (66 assertions in 2 test cases)". The companion case at :126 remains at 3 cycles, which is a different (non-SC-016) arm. |
| SC-017 | sc | PASS | seraphis_tests.exe "The effects send is block-size invariant" PASSES. All seven partitions from effects_chain_test.cpp:433 kInvariancePartitions[] = {1, 2, 3, 7, 512, 2048, 4096} over kInvarianceTotalSamples = 192000 (:410, = 4 s @ 48 kHz), reference block 512 (:421): the log reports "partition 1: worst \|diff\| L=0 R=0", and identically for 2, 3, 7, 2048 and 4096 — every CHECK(dl <= kSampleTolerance) / CHECK(dr <= kSampleTolerance) at :2278-2279 passing with an exact zero against the 1.0e-4 tolerance. Reference non-vacuous: referencePeak 0.25193f > 1e-4. The mandatory negative control fires: with the accumulator bypassed (send fed the raw slice length), "raw-send partition divergence: L=1.91363 R=1.93488 (tolerance 0.0001)" against a raw-send reference peak of 1.04554 — CHECK(rawDiffL > kSampleTolerance) and CHECK(rawDiffR > kSampleTolerance) both pass, so the criterion cannot pass vacuously. |
| SC-018 | sc | PASS | seraphis_tests.exe "Effects push cadence" (param_cadence_test.cpp) PASSES, all five clauses as named sections. (a) :1348 REQUIRE(after.resets == before.resets + 1u) for a qualifying engage, with REQUIRE(before.resets == 0); :1371 freeze-FORCED engage CHECK(after.resets == std::size_t{0}); :1393 sub-drain excursion CHECK(after.resets == engaged.resets). (b) :1422 REQUIRE(after.driftBlocks == before.driftBlocks + kDefaultsBlocks) and REQUIRE(both.driftBlocks == engaged.driftBlocks + kFxObserveBlocks) — equal under both bypass states. (c) :1461 REQUIRE(after.predicateEvals == before.predicateEvals + kSlicedBlocks) over a MIDI-subdivided render, with REQUIRE(after.stageCalls == before.stageCalls + kSlicedBlocks) pinning the per-call divisor. (d) :1529 REQUIRE(after.voice == before.voice) and REQUIRE(after.aether == before.aether) while REQUIRE(after.effectsPushes >= before.effectsPushes + kFxCountedPackRows); ID 1400 moves only engSoftLimit (+1u), neither voice nor Aether. (e) :1599 REQUIRE(bus.sendChunks == 0) at the C-6 defaults; :1616 CHECK(lastIncreaseBlock <= kBypassBlock + kDrainWindowBlocks + 1u) with CHECK(lastIncreaseBlock >= kBypassBlock). |
| SC-019 | sc | PASS (with scope caveat) | seraphis_tests.exe "Synced delay tracks host tempo" PASSES, four sections including the label-table tie and the invalid-transport fallback. Measured vs expected echo period of the isolated send return at preOutputTapForTest(), tolerance +/- one hop = +/- 512 samples: expected 5142.86 -> measured 5120 (err 22.9); expected 6857.14 -> 7168 (err 310.9); expected 8000 -> 8192 (err 192); expected 10666.7 -> 10752 (err 85.3); and the invalid-transport 120 BPM fallback expected 6000 (125 ms = index 7 = "1/16") -> 6144 (err 144). All five inside 512. Autocorrelation peak/zero-lag ratios 0.47-0.49 confirm a real comb, and the section "the period estimator recovers a known comb" validates the estimator itself. CAVEAT: this pass depends on an uncommitted dsp/ behaviour change (see SC-015 note in my report) — spectral_delay.h's delayFrames now subtracts one frame, which the spec's Non-goals forbid. |
| CC-rt | cc | PASS | BOTH CONSTITUTION II DEFECTS ARE FIXED; the third finding was mischaracterized and is spec-designed and measured. FIX 1+2 — the unconditional per-slice std::chrono::steady_clock::now() pair AND the unconditional full-bus preOutTap copy are now behind a runtime gate. processor.cpp:1911 `const bool instrumented = effectsStageInstrumented_;` guards the start clock (:1913-1915), the tap copy (:1935-1940) and the end clock + accumulate (:1942-1947). The flag is FALSE on every shipping path and provably so: processor.h:1061 `bool effectsStageInstrumented_ = false;` is the declaration, processor.h:474-475 `setEffectsStageInstrumentedForTest(bool)` is the ONLY writer in the entire tree, and a repo-wide grep for `effectsStageInstrumented_\|setEffectsStageInstrumented` across plugins/seraphis/src and plugins/seraphis/tests returns exactly one caller — seraphis_test_fixture.h:210. The shipped plugin therefore executes no clock read and no copy here, paying one always-false branch per slice; the comment at :1894-1904 states the reasoning and the Constitution II citation explicitly. FINDING 3 REASSESSED — spectralDelay_.reset() on the audio thread (processor.cpp:2056 fxResetDue_ path, :1696 seed-change path, :858 deactivate path) is FR-008's deferred-reset design, not an unbudgeted defect, and it is allocation-free (spectral_delay.h:242-291 is std::fill + DelayLine::reset + RNG draws only, no allocation). Its cost is no longer estimated but MEASURED and GATED by SC-011 across my seven idle runs: worst FR-008 reset block 117 300 ns = 1.100 % of one core and worst seedRng()+reset() block 9 500 ns = 0.089 %, both against the 533 333 ns / 5 %-of-core ceiling — 22 % and 1.8 % of budget. (My earlier ~2 MiB memset estimate is inconsistent with a 9.5 us measured seed block and I do not stand behind it; the measurement is the evidence.) RT-SAFETY POSITIVELY PROVEN AT RUNTIME AND BY SOURCE SCAN: over a 60 s render (5625 x 512-sample blocks) inside TestHelpers::AllocationScope, allocations == 0, exceptions == 0 (real try/catch counter, effects_perf_test.cpp:872-879), and the 6-file audio-thread source scan reports lockPrimitives == 0 and throwSites == 0 across 11 lock and 5 throw token spellings with a positive/negative control proving the scanner is not vacuous. All allocation remains confined to setupProcessing() (processor.cpp:594-595, :616, :643-648, :690-691, :705). |
| CC-layers | cc | PASS | NO new DSP headers were created — `git status --porcelain dsp/` returns exactly two modified files and zero additions: dsp/include/krate/dsp/effects/spectral_delay.h and dsp/include/krate/dsp/primitives/stft.h. spectral_delay.h (Layer 4, effects) includes only core/block_context.h, core/db_utils.h, core/math_constants.h, core/note_value.h, core/random.h (Layer 0, lines 28-32), primitives/delay_line.h, primitives/smoother.h, primitives/spectral_buffer.h, primitives/stft.h (Layer 1, lines 33-36) and systems/delay_engine.h (Layer 3, line 37) — no Layer-4 include, all downward. stft.h (Layer 1, primitives) includes fft.h and spectral_buffer.h (same-layer peers, lines 19-20) and ../core/window_functions.h (Layer 0, line 21) — unchanged by this phase, the diff adds only a capacity guard at stft.h:328-345. The new plugin-local header plugins/seraphis/src/parameters/effects_params.h is not a DSP layer and may consume any layer; it includes effects/spectral_delay.h, processors/brownian_drift.h, processors/midside_processor.h, systems/seraphis_engine.h (effects_params.h:36-39). ODR sweep for new type names: `grep -rn "SeraphisEffectsStageBypassProbe\|struct FxSendState\|class FxSend" dsp/ plugins/` -> only the Seraphis declarations (processor.h:276 forward decl, :457 friend, :705 enum class FxSendState) plus the test-side definition (effects_chain_test.cpp:127); `grep -rn "EffectsParams" dsp/ plugins/ --include=*.h \| grep -v seraphis` -> zero hits. |
| CC-naming | cc | PASS | NAMING VERIFIED FRESH THIS SESSION, AND THE HYGIENE DEFECT THAT BLOCKED THE PASS IS RESOLVED. Parameter IDs follow k{Section}{Parameter}Id with the canonical names — plugin_ids.h:185-200 reads kFxSaturationId=1400, kFxDelayMixId=1410 ("Mix", per CLAUDE.md's table, not "DryWet"), kFxDelayTimeId=1411, kFxDelaySpreadId=1412, kFxDelaySpreadDirectionId=1413, kFxDelayFeedbackId=1414, kFxDelayTiltId=1415, kFxDelayDiffusionId=1416, kFxDelayWidthId=1417, kFxDelaySyncId=1418, kFxDelaySyncNoteId=1419, kFxSpectralFreezeId=1430, kFxWidthId=1440, kFxWanderDepthId=1441, kFxWanderRateId=1442, kFxAzimuthDepthId=1443 — 16 rows, contiguous banding. Constants are kPascalCase: plugin_ids.h:280 `constexpr Steinberg::Vst::ParamID kEffectsParamRangeEnd = 1500;` sits in the range ladder beside kAtmosParamRangeEnd/kAetherParamRangeEnd; processor.h:145 kFxSendChunkSamples, :216 kWanderControlChunkSamples, :230 kFxFifoCapacity = 4096 with :231 static_assert((kFxFifoCapacity & (kFxFifoCapacity - 1u)) == 0u, "the ring index is a mask") and :232 the capacity floor. Members carry the trailing underscore: processor.h:708-710 `std::vector<float> fxInL_, fxInR_; fxOutL_, fxOutR_; fxChunkL_, fxChunkR_;`, :1074 `std::vector<float> preOutTapL_, preOutTapR_;`, :1061 effectsStageInstrumented_, plus spectralDelay_/widthDrift_/azimuthDrift_/globalMs_. Functions camelCase (runSendStage, runWanderStage, updateEffectsBypassState, pushEffectsParams, setEffectsStageInstrumentedForTest); classes/structs PascalCase (EffectsParams, RtSourceScan, RtTrial, PerBlockBest). HYGIENE DEFECT CLEARED: the fourteen stray shell-quoting artifacts previously sitting in the repo root are GONE. `git status --porcelain -uall` now lists only intended paths — 24 modified (incl. tools/check-portability.js and tools/run-clang-tidy.ps1) and 7 untracked, all under plugins/seraphis/, dsp/ or specs/seraphis-phase10-effects/ — and filtering that output for non-plugins/specs/dsp/tools paths returns NOTHING. `ls -la` on the root shows no `0.0)`, `1217`, `candidates`, `float`, `n` or mojibake entries; the only loose objects are probe.obj/vel2.obj/vel3.obj dated Jul 24-28, i.e. predating this phase's Aug 2-3 window and untracked-clean. A `git add -A` is now safe. |
| CC-warnings | cc | PASS | THE ONE BLOCKER — an unobserved seraphis_tests link — IS CLEARED, because the machine is no longer contended. `tasklist \| grep -i "seraphis\|cl.exe\|MSBuild\|cmake"` returned NOTHING before I built (the two resident seraphis_tests.exe instances, PIDs 2516/11440, that caused the three prior LNK1104 failures are gone). Incremental build proved nothing again (first run recompiled 0 TUs), so I forced recompilation by touching all 22 changed/new sources plus both changed DSP headers, then rebuilt. RESULT 1 — /f/tmp/rv_build2.log: EXIT=0, `grep -ci warning` = 0, 24 TUs recompiled (controller.cpp, processor.cpp, entry.cpp, pitch_shift_processor.cpp, effects_chain_test.cpp, effects_perf_test.cpp, state_v3_test.cpp, param_perf_test.cpp, param_cadence_test.cpp, param_continuity_test.cpp, param_denorm_test.cpp, parameter_surface_test.cpp, processor_bus_test.cpp, state_roundtrip_test.cpp, state_v2_test.cpp, lifecycle_test.cpp, editor_lifecycle_test.cpp, macro_wiring_test.cpp, midi_event_test.cpp, morph_sync_test.cpp, param_character_test.cpp, param_flow_test.cpp, param_reach_test.cpp, processor_audio_test.cpp), AND BOTH LINKS COMPLETED: line 41 "seraphis_tests.vcxproj -> F:\\projects\\iterum\\build\\windows-x64-release\\bin\\Release\\seraphis_tests.exe", line 63 "Seraphis.vcxproj -> ...\\VST3\\Release\\Seraphis.vst3\\Contents\\x86_64-win\\Seraphis.vst3". RESULT 2 — /f/tmp/rv_dspbuild.log: all five DSP layer suites rebuilt after the stft.h + spectral_delay.h touch, EXIT=0, `grep -ci warning` = 0, 28 TUs. SUITES ALL GREEN (run by me from these binaries): seraphis_tests.exe EXIT=0 "All tests passed (379341 assertions in 46 test cases)"; dsp_core_tests EXIT=0 "All tests passed (1588257 assertions in 573 test cases)"; dsp_primitives_tests EXIT=0 "(4549290 assertions in 1505 test cases)"; dsp_processors_tests EXIT=0 "(10640741 assertions in 3296 test cases)"; dsp_systems_tests EXIT=0 "(6042460 assertions in 1218 test cases)"; dsp_effects_tests EXIT=0 "(98155 assertions in 477 test cases)"; plus seven consecutive `seraphis_tests.exe "[.perf]"` runs, all EXIT=0, all "All tests passed (504 assertions in 7 test cases)". Note this is MSVC only — GCC/AppleClang warning-freedom remains unverifiable on this host (see SC-015). |
| CC-portability | cc | PASS | Ran `node tools/check-portability.js` (g++ via WSL, -fsyntax-only). VERBATIM RESULT: "check-portability: 12 translation unit(s) with g++ / OK plugins/seraphis/src/controller/controller.cpp / OK plugins/seraphis/src/processor/processor.cpp / OK plugins/seraphis/tests/integration/param_cadence_test.cpp / OK plugins/seraphis/tests/integration/param_continuity_test.cpp / OK plugins/seraphis/tests/integration/param_perf_test.cpp / OK plugins/seraphis/tests/unit/controller/editor_lifecycle_test.cpp / OK plugins/seraphis/tests/unit/lifecycle_test.cpp / OK plugins/seraphis/tests/unit/param_denorm_test.cpp / OK plugins/seraphis/tests/unit/parameter_surface_test.cpp / OK plugins/seraphis/tests/unit/processor_bus_test.cpp / OK plugins/seraphis/tests/unit/state_roundtrip_test.cpp / OK plugins/seraphis/tests/unit/state_v2_test.cpp / check-portability: all clear -- 12 compiled." GAP FOUND AND CLOSED: the default invocation diffs against origin/main and therefore SKIPPED the three UNTRACKED new test TUs. I re-ran it explicitly on them — VERBATIM: "check-portability: 3 translation unit(s) with g++ / OK plugins/seraphis/tests/integration/effects_chain_test.cpp / OK plugins/seraphis/tests/integration/effects_perf_test.cpp / OK plugins/seraphis/tests/unit/state_v3_test.cpp / check-portability: all clear -- 3 compiled." 15 TUs total, all green under GCC. The two changed DSP headers (spectral_delay.h, stft.h) are covered transitively via processor.cpp and effects_chain_test.cpp. Spot checks of the specific hazards: no std::isnan in the new code, no narrowing in brace init (all designated/value init — Krate::DSP::BlockContext fxBlockCtx_{} at processor.h, std::array<...> {} initializers at :768-770), no SIMD loads added, and the new floats carry explicit f suffixes (e.g. kFxAzimuthCentreComp = 1.41421356f, processor.h:193; kFxSendDrainFloor = 1.0e-6f, :160). |

**Tally:** 95 rows — 93 pass, **2 fail** (SC-015 under both lenses; it is one defect counted once per lens).

---

## Implementation notes — deviations reported by task agents

### Spec / plan (T001)
All four escalations (OQ-1..OQ-4 = D-1..D-4) were ruled in favour of the plan's recommendation, so
no downstream task changed. Rulings recorded in spec.md under "Phase-owner rulings on the plan's
escalations - 2026-08-02"; FR-017/C-6 row 1419 default moved 4→7, FR-021 (D-2), FR-003a/FR-007/
FR-008/C-3/SC-017 amended (D-3, Q5 marked SUPERSEDED), C-5/FR-024a/edge cases (D-4). Editorial
amendments FR-040 (three probe capabilities) and FR-041 (seven surfaces + the truncation flag)
landed. **Residual R-12 stays OPEN:** the amendment made FR-008 honest, it did not make the chunk
phase absolute.

### Parameter surface (T002–T007)
- **T003 — arrow encoding deviation (behavioural).** The task specified `STR16("Low \xE2\x86\x92 High")`,
  but `STR16(x)` is `u ## x`, so hex escapes are UTF-16 *code units*, not UTF-8 bytes — that literal
  would have shipped mojibake and compiled silently. Shipped `STR16("Low → High")` via a UCN
  instead; file verified pure ASCII. Also added an explicit `note_value.h` include (plan listed only
  `spectral_delay.h`).
- **T004 — two judgement calls.** ID 1414's display format is unspecified anywhere in
  spec/plan/tasks; shipped `%.0f%%` over the *denormalized* value (35 % at default). And
  `formatEffectsParam` **claims** its two dropdowns (1413/1419), unlike every other Seraphis pack.
- **T005 — round-trip clause corrected.** The task's "every ID over {0, 0.25, 0.5, 0.75, 1.0}" is
  false for the two StringListParameter rows (`FromNormalized` truncates over `stepCount+1`
  buckets: at 1419, 0.25 → plain 2 → 0.2222). The two L rows are asserted over their canonical step
  values instead — the same distinction the file already documents for `kPolyphonyId`.
- **T006 — `entries` semantics.** The task said `Kind::L, 2` / `Kind::L, 9`; `SurfaceRow::entries`
  is the entry *count* (`stepCount == entries - 1`), so `3` / `10` were written. An **eleventh
  FR-038a obligation** was also found and fixed in that file: the SC-001 ID-set loop carried
  `CHECK(info.id < kAetherParamRangeEnd)`, which fails for all 16 effects IDs; bound changed to
  `kEffectsParamRangeEnd`.
- **T007 — flagged, later fixed.** The harness ran `/*cycles=*/3`, not SC-016's 10. T029 escalated
  the same defect; the final tree runs 10 (editor_lifecycle_test.cpp:253) and SC-016 passes.

### Processor + state (T008–T014, T016–T017)
- **T008 — `lastPushedSoftLimitValid_` removed** (it became write-only once the `pushGlobalParams()`
  saturation block was deleted); `lastPushedSoftLimit_` kept and still live.
  **Collateral flagged and since resolved:** `state_v2_test.cpp` asserted `getSize() == 2532` in 13
  places against a now-2596-byte blob; FR-038a enumerated only four breaking test files and named
  none of these.
- **T011 — three judgement calls.** SC-002's "one Processor instance" needed a documented
  `reprepare()` between the two renders (a live reverb tail otherwise makes exact equality
  unachievable for a *correct* implementation); the 24 s ceiling for the loud clause is anchored to
  a measured precedent (param_flow_test.cpp:369); two stale banner comments corrected.
- **T013 — seven notes.** SC-017's negative control is realised at the component level rather than
  as a fourth probe capability (a probe branch would place FR-003a's prohibited call shape into the
  shipping binary); SC-017's "single contiguous render" is the 512-sample reference partition;
  SC-017 **gates** on max per-sample diff ≤ `kSampleTolerance` with `compareFingerprints()` as a
  warn-only secondary (precedent: midi_event_test.cpp:426-431). Runtime cost noted: 7 × 4 s renders
  including block sizes 1/2/3.
- **T014 — one line outside the literal body list:** `lastPushedFxValid_ = false;` in
  `pushAllSurfaces()`, because `setupProcessing()` otherwise leaves the component on its own
  construction defaults (feedback 0, diffusion 0, width 0, note index 4) rather than the registered
  C-6 set. Also: three read-only statics added to the existing friend probe rather than new public
  `*ForTest()` accessors, to keep FR-041's surface closed at seven + the truncation flag. FR-023 arm
  shapes were re-derived because "no held send tail" is not decidable as audio (the send is fed the
  post-Aether bus).
- **T016 — five deviations.** SC-011a's "with a captured freeze" is mutually exclusive with its own
  mandatory control under FR-023a, so it was split into three arms (freeze-on short excursion;
  freeze-off short vs long control pair). The control excursion is 5.01 s, not 2 s, because
  `fxBypassedSamples_` only accumulates *after* the drain window ends. "RMS 500 ms after
  re-engagement" is a 500 ms window *starting* at +500 ms (a window starting at re-engagement
  contains the recirculating silence hole and a correct build measures ≈ −2.4 dB there). SC-007's
  engage block moved to ~3 s so `resets == 0` is discriminating. Freeze capture is asserted as an
  exact block count in **both** directions.
- **T017 — six deviations.** (1) `wanderAtIdentity()`'s azimuth clause uses `|g − 1| ≤ 1e-6f`, not
  `== 1.0f`: `cos(0.5f·kHalfPi) * kFxAzimuthCentreComp` rounds to `0.99999994f`, exactly one ulp
  below unity, so an exact test would make FR-010's mandatory skip unreachable forever. (2)
  FR-010a's engage arm snaps `globalMs_` to *identity*, not to the new width (snapping to the new
  target is itself the step SC-008 forbids). (3) **The BrownianDrift advance moved from one
  per-block call to one per absolute 64-sample grid boundary** — the plan's per-block shape makes
  the drift staircase a function of host block length and would have failed SC-017 by ~2-5
  percentage points of width. (4) A probe-only control-grid witness (~600 B) was added because
  FR-024's "at most 32 distinct target values" is otherwise unobservable in audio. (5) FR-024a's
  "same-block skip" was split into a raw-predicate claim and a latched-effective claim, because as
  written it contradicts FR-010a's disengage latch. (6) SC-003(c)'s width-0 clause is a relative
  bound, not `== 0.0`.

### Test suite (T015, T018–T025)
- **T015 — measurement design.** SC-019 measures echo *spacing* via autocorrelation (an arrival time
  is read through the component's own ~21 ms smearing against a ±10.67 ms tolerance); a 22 s settle
  precedes the burst (the base-delay smoother's τ ≈ 5.12 s), deliberately *not* pre-snapped so a
  transport-ignoring build still fails. Script isolation is asserted, not assumed. Wall-time
  warning: SC-005 renders 4 × 121 s and SC-019 12 × ~25 s.
- **T018 — probe method added** (`swapDriftSalts`) because the salts are `inline constexpr` and
  cannot be rebound by a test; applied after block 0 so the seed burst cannot overwrite it. All 16
  IDs at maxima means the freeze is ON for the whole SC-006 render (a boolean's maximum).
- **T019 — one deliberate schedule choice.** Send-bypass is scheduled exactly
  `kClickWindowShiftSamples` after the wander disengage so the return-gain ramp edge lands inside a
  measured window; otherwise positive control (b) would be structurally incapable of failing. This
  makes the render strictly *harder* to pass; a `static_assert` pins the pairing.
- **T020 — deliberate omissions stated rather than papered over.** No arm touches `kSeedId` (the
  seed burst shares `spectralDelayResets_`); IDs 1441/1443 have no push counter and are witnessed by
  effects_chain_test.cpp rather than by a flaky short-render assertion.
- **T021 — a real defect was traced and handed off.** `SpectralDelay::reset()` does not rewind its
  RNG (it consumes 2·numBins draws for stereo phases, spectral_delay.h:279-284), so
  `setActive(false)`'s bare `reset()` was not seed-deterministic. Prescribed fix: `seedRng(...)`
  immediately before `reset()`. The final tree carries it (FR-027 evidence cites processor.cpp:857-858).
  Also: the file uses the explicit-instantiation access idiom rather than an eighth `*ForTest()`
  accessor, and a vacuous M/S render clause was written and then **deleted** rather than shipped.
- **T022 — arm 2 holds 1441/1443 at 0** so the installed width is exactly the loaded width (the
  wander adds an unobservable drift term); those are the two rows `pushEffectsParams()` does not
  touch, so the path under test is unaffected.
- **T023 — the edit was already present** in the working tree (a retried earlier run); verified
  rather than re-applied. Two citation deltas from the task table are the *more* accurate strings.
  **Recorded, not acted on:** the `dsp/spectral_delay.h` edit shifts every line after ~345 by ~+44,
  so FR-038b's citations (written against HEAD) no longer point at the same lines in the working
  copy — a spec-level decision, flagged rather than silently re-numbered.
- **T024 — baseline table shipped as a labelled placeholder** ("** NOT YET FILLED **") because the
  agent was forbidden to build or run; nothing gated on it. **Since filled** — see SC-012/SC-013.
  SC-011 holds 2 voices (justified in-file: the timed region reads no voice state); its five
  statistics are each best-of-16 separately.
- **T025 — "most-expensive end" made operational, not asserted.** A new `[.perf]` case
  `Seraphis_EffectsTable_IsTheWorstCase` measures every candidate of all seven ambiguous rows and
  REQUIREs the transcribed value within a documented 10 % tie band (a strict arg-max on wall-clock
  figures would be a flake generator). `applyTableEngValues` now pushes the table's 1400 value
  instead of the compile-time `kOutputSaturation`, per D-2. The 25 % gate constants are byte-identical.

### Verification tasks (T026–T030)
- **T026 (build/run) and T029 (build + pluginval) returned `blocked`** — both are pure execution
  tasks with empty file lists, and the implementation agents are forbidden to build. Both were
  subsequently executed (see CC-warnings, SC-016).
- **T027 — two coverage holes in the literal command, closed manually.** `check-portability.js`
  derives its file list from `git diff` and therefore skipped the three **untracked** new TUs
  (re-run explicitly: "all clear -- 3 compiled"); and the two modified `dsp/` headers are not TUs,
  so their consumers were run explicitly ("all clear -- 4 compiled"). **Observation not acted on:**
  `lint-nonfinite-symbols.js`'s GUARDED array is an Atmosphere/Aether allowlist — no Phase 10 file
  is on it, so its green says nothing about this phase; the manual grep is the actual evidence.
- **T028 — 5 clang-tidy warnings in `plugins/seraphis/src/processor/processor.cpp` fixed** (De
  Morgan on the wander predicate; `std::max({...})`; `modernize-use-auto`; `std::cmp_greater` for
  the saturating `uint64` vs `int64` compare — also the semantically correct fix; merged branch
  clone with the FR-019 rationale hoisted to a banner). Re-run: 0/0 over `src`. No `.sh` edit was
  needed (it already carried the seraphis case). **This is the `src` tree only — the 924 SC-015
  warnings are all in `tests/`.**
- **T030 — `version.json` deliberately not bumped** (outside the file list). CHANGELOG carries
  `## [0.3.0] - 2026-08-03`; the Performance section states gates rather than fabricated measured
  percentages, because that agent could not run anything.

---

## Remaining gates for the human loop

1. **clang-tidy — BLOCKING, currently red.**
   `pwsh ./tools/run-clang-tidy.ps1 -Target seraphis -BuildDir build/windows-ninja` →
   Files analyzed 25 / Errors 0 / **Warnings 924**, all in `plugins/seraphis/tests/`. 52 are in
   Phase 10's own new files. SC-015 is not satisfiable until this is 0 (or an explicitly justified
   suppression lands). The dominant check is `modernize-use-designated-initializers` (851).
   Note that `tools/run-clang-tidy.ps1` was amended this phase to analyze the tests tree so it
   matches `run-clang-tidy.sh:151-152` — reverting that amendment would hide the breach, not fix it.

2. **Phase-owner ruling on the `dsp/` scope violation — BLOCKING before commit.**
   `dsp/include/krate/dsp/effects/spectral_delay.h` and
   `dsp/include/krate/dsp/primitives/stft.h` are modified; the spec's Non-goals forbid it, and the
   delay-time change reaches Iterum and Ruinae. Either accept it explicitly (and re-run Iterum's
   `plugin_tests` + `approval_tests` and Ruinae's `ruinae_tests`, plus their pluginval) or revert
   it and re-establish SC-019 another way.

3. **pluginval — required (plugin code changed; this is a phase-8+ plugin phase). Already run
   green** against the bundle rebuilt from current sources:
   `tools/pluginval.exe --strictness-level 5 --validate build/windows-x64-release/VST3/Release/Seraphis.vst3`
   → EXIT=0, 19/19 groups completed, 0 failures. **Re-run it** if the clang-tidy fixes or the `dsp/`
   ruling change any shipping source.

4. **Release bookkeeping (optional for this phase, required before shipping 0.3.0):**
   `plugins/seraphis/version.json` is still `0.2.0` while CHANGELOG carries `## [0.3.0]`.
   Bump `version.json` only — never `version.h`, `win32resource.rc` or `audiounitconfig.h`.

5. **Commit** — only after gates 1 and 2 are resolved. Nothing in this phase has been committed;
   `git status --porcelain -uall` shows 24 modified + 7 untracked paths, all under
   `plugins/seraphis/`, `dsp/`, `tools/` or `specs/seraphis-phase10-effects/`.
